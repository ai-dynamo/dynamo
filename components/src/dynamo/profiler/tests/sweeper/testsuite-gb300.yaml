# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

tests:
  - case: qwen3-32b-vllm-agg
    hardware: gb300-4gpu
    exceptions:
      render:
        profiler-v1beta1:
          status: skipped
          reason: DGDR v1beta1 does not define gb300 in its gpuSku enum.
      deploy:
        sweeper-aic:
          status: skipped
          reason: Sweeper's vLLM performance-data version is not yet bound to an exact, deploy-validated Dynamo GB300 runtime.
        sweeper-direct:
          status: skipped
          reason: Sweeper's vLLM performance-data version is not yet bound to an exact, deploy-validated Dynamo GB300 runtime.
        recipe:
          status: skipped
          reason: The maintained recipe is validated for H200, not GB300.
