# SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
    helm.sh/resource-policy: keep
  name: dynamographdeploymentenginegroups.nvidia.com
spec:
  group: nvidia.com
  names:
    kind: DynamoGraphDeploymentEngineGroup
    listKind: DynamoGraphDeploymentEngineGroupList
    plural: dynamographdeploymentenginegroups
    shortNames:
      - dgdeg
    singular: dynamographdeploymentenginegroup
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: Desired logical replicas
          jsonPath: .spec.replicas
          name: DESIRED
          type: integer
        - description: Logically complete physical allocations
          jsonPath: .status.replicas
          name: ALLOCATED
          type: integer
        - description: Available logical replicas
          jsonPath: .status.availableReplicas
          name: AVAILABLE
          type: integer
        - description: Engine-committed logical replicas
          jsonPath: .status.activeReplicas
          name: ACTIVE
          type: integer
        - description: Unit counted by desired and observed replica columns
          jsonPath: .status.scaleUnit
          name: UNITS
          type: string
        - description: Physical Pods allocated per logical replica
          jsonPath: .status.profile.podsPerReplica
          name: PODS/REPLICA
          type: integer
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: |-
            DynamoGraphDeploymentEngineGroup represents one independently resizable distributed engine world.
            The scale subresource counts logical replicas, while status keeps physical allocation, engine
            membership, and traffic admission separately observable.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: |-
                DynamoGraphDeploymentEngineGroupSpec defines the desired capacity of one independently
                resizable distributed engine world.
              properties:
                policy:
                  description: |-
                    policy constrains user- or autoscaler-selected replica targets independently from hard
                    engine capability bounds reported in status.profile.
                  properties:
                    maxReplicas:
                      description: |-
                        maxReplicas is the maximum target selected by policy. It cannot exceed the resolved hard
                        engine capability bound.
                      format: int32
                      minimum: 1
                      type: integer
                    minReplicas:
                      description: |-
                        minReplicas is the minimum ordinary scaling target. Terminal group retirement is driven by
                        deletion and may retire membership below this bound without writing an out-of-policy target.
                      format: int32
                      minimum: 1
                      type: integer
                  type: object
                  x-kubernetes-validations:
                    - message: minReplicas must be less than or equal to maxReplicas
                      rule: '!has(self.minReplicas) || !has(self.maxReplicas) || self.minReplicas <= self.maxReplicas'
                replicas:
                  description: |-
                    replicas is the absolute desired number of logical engine replicas in this group.
                    For Elastic EP, one replica maps to one data-parallel replica through the resolved profile.
                  format: int32
                  minimum: 1
                  type: integer
              required:
                - replicas
              type: object
              x-kubernetes-validations:
                - message: replicas must be greater than or equal to policy.minReplicas
                  rule: '!has(self.policy) || !has(self.policy.minReplicas) || self.replicas >= self.policy.minReplicas'
                - message: replicas must be less than or equal to policy.maxReplicas
                  rule: '!has(self.policy) || !has(self.policy.maxReplicas) || self.replicas <= self.policy.maxReplicas'
            status:
              description: DynamoGraphDeploymentEngineGroupStatus defines the observed state of one Engine Group.
              properties:
                activeReplicas:
                  description: activeReplicas is the number of replicas in the engine's authoritative committed topology.
                  format: int32
                  minimum: 0
                  type: integer
                availableReplicas:
                  description: |-
                    availableReplicas is the number of allocated replicas whose complete physical allocation
                    and profile-required runtime checks are available.
                  format: int32
                  minimum: 0
                  type: integer
                conditions:
                  description: |-
                    conditions contains the latest observations of group availability, progress, degradation,
                    target convergence, target validity, and topology authority.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastStableReplicas:
                  description: |-
                    lastStableReplicas is the most recent membership count that reached its desired target
                    without degradation.
                  format: int32
                  minimum: 0
                  type: integer
                observedGeneration:
                  description: observedGeneration is the most recent object generation observed by the controller.
                  format: int64
                  minimum: 0
                  type: integer
                profile:
                  description: profile is the immutable resolved mapping between logical replicas and physical capacity.
                  properties:
                    backend:
                      description: backend is the inference engine that owns native membership.
                      enum:
                        - sglang
                        - vllm
                        - trtllm
                      type: string
                    fingerprint:
                      description: fingerprint identifies the immutable engine and workload geometry used by this group.
                      minLength: 1
                      type: string
                    gpusPerReplica:
                      description: gpusPerReplica is the accelerator requirement of one logical replica.
                      format: int64
                      minimum: 1
                      type: integer
                    maxReplicas:
                      description: maxReplicas is the hard upper bound for live membership operations.
                      format: int32
                      minimum: 1
                      type: integer
                    minReplicas:
                      description: |-
                        minReplicas is the hard lower bound for live membership operations other than terminal
                        group retirement.
                      format: int32
                      minimum: 1
                      type: integer
                    minSafeServingReplicas:
                      description: |-
                        minSafeServingReplicas is the lowest committed replica count at which this profile may
                        continue serving while degraded or recovering.
                      format: int32
                      minimum: 1
                      type: integer
                    podsPerReplica:
                      description: |-
                        podsPerReplica is the number of physically disjoint capacity Pods allocated and released
                        together for one logical replica.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                    - backend
                    - fingerprint
                    - gpusPerReplica
                    - maxReplicas
                    - minReplicas
                    - minSafeServingReplicas
                    - podsPerReplica
                  type: object
                reconciliation:
                  description: |-
                    reconciliation is the controller's durable desired, accepted, and observed journal.
                    It is persisted before external effects so the same transition can resume after restart.
                  properties:
                    capacity:
                      description: capacity records the newest desired target, the last accepted target, and physical observation.
                      properties:
                        accepted:
                          description: accepted is the last exact target durably acknowledged by the capacity adapter.
                          properties:
                            controlRevision:
                              description: controlRevision orders this target within the Engine Group.
                              format: int64
                              minimum: 1
                              type: integer
                            processLifecycleOwner:
                              description: processLifecycleOwner identifies who starts and stops engine processes.
                              enum:
                                - Engine
                                - Orchestrator
                              type: string
                            profileFingerprint:
                              description: profileFingerprint binds the target to immutable engine and workload geometry.
                              minLength: 1
                              type: string
                            releaseFences:
                              description: releaseFences authorize removal of exact Pod UIDs from exact stable slots.
                              items:
                                description: EngineGroupReleaseAuthorization permits removal of only named concrete Pod incarnations.
                                properties:
                                  capacityRefs:
                                    description: capacityRefs is the complete set of concrete Pod UIDs authorized for deletion.
                                    items:
                                      description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                      properties:
                                        name:
                                          description: name is the Pod name.
                                          minLength: 1
                                          type: string
                                        uid:
                                          description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                          type: string
                                      required:
                                        - name
                                        - uid
                                      type: object
                                      x-kubernetes-validations:
                                        - message: uid must not be empty
                                          rule: size(self.uid) > 0
                                    minItems: 1
                                    type: array
                                  operationID:
                                    description: operationID identifies the membership result authorizing release.
                                    minLength: 1
                                    type: string
                                  replicaID:
                                    description: replicaID is the stable logical replica being released.
                                    minLength: 1
                                    type: string
                                  slotID:
                                    description: slotID is the stable workload-manager position being fenced.
                                    minLength: 1
                                    type: string
                                  topologyGeneration:
                                    description: topologyGeneration identifies the exact committed topology authorizing release.
                                    format: int64
                                    minimum: 1
                                    type: integer
                                required:
                                  - capacityRefs
                                  - operationID
                                  - replicaID
                                  - slotID
                                  - topologyGeneration
                                type: object
                              type: array
                            replicas:
                              description: replicas is the complete desired set of physical replica allocations.
                              items:
                                description: EngineGroupCapacityReplicaTargetStatus describes one allocation required by an absolute target.
                                properties:
                                  bootstrap:
                                    description: bootstrap authorizes creation of a new orchestrator-owned incarnation.
                                    properties:
                                      baseTopologyGeneration:
                                        description: baseTopologyGeneration binds bootstrap to the topology from which the change starts.
                                        format: int64
                                        minimum: 1
                                        type: integer
                                      mode:
                                        description: mode distinguishes fresh joining capacity from fixed-slot restoration.
                                        enum:
                                          - Join
                                          - RestoreFixedSlot
                                        type: string
                                      nativeMembers:
                                        description: nativeMembers names the fixed backend identities when the bootstrap mode requires them.
                                        items:
                                          minLength: 1
                                          type: string
                                        type: array
                                    required:
                                      - baseTopologyGeneration
                                      - mode
                                    type: object
                                  incarnation:
                                    description: incarnation asserts the exact existing capacity that must remain assigned.
                                    properties:
                                      capacityRefs:
                                        description: capacityRefs contains every concrete Pod in this replica allocation.
                                        items:
                                          description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                          properties:
                                            name:
                                              description: name is the Pod name.
                                              minLength: 1
                                              type: string
                                            uid:
                                              description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                              type: string
                                          required:
                                            - name
                                            - uid
                                          type: object
                                          x-kubernetes-validations:
                                            - message: uid must not be empty
                                              rule: size(self.uid) > 0
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process incarnation.
                                        minLength: 1
                                        type: string
                                      slotID:
                                        description: slotID is the stable workload-manager position.
                                        minLength: 1
                                        type: string
                                    required:
                                      - capacityRefs
                                      - replicaID
                                      - runtimeIncarnation
                                      - slotID
                                    type: object
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  slotID:
                                    description: slotID identifies the stable workload-manager position.
                                    minLength: 1
                                    type: string
                                required:
                                  - replicaID
                                  - slotID
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                                - replicaID
                              x-kubernetes-list-type: map
                            transitionID:
                              description: transitionID correlates this target with one membership transition.
                              minLength: 1
                              type: string
                          required:
                            - controlRevision
                            - processLifecycleOwner
                            - profileFingerprint
                            - transitionID
                          type: object
                        desired:
                          description: desired is the newest persisted absolute target, including one that may later be rejected.
                          properties:
                            controlRevision:
                              description: controlRevision orders this target within the Engine Group.
                              format: int64
                              minimum: 1
                              type: integer
                            processLifecycleOwner:
                              description: processLifecycleOwner identifies who starts and stops engine processes.
                              enum:
                                - Engine
                                - Orchestrator
                              type: string
                            profileFingerprint:
                              description: profileFingerprint binds the target to immutable engine and workload geometry.
                              minLength: 1
                              type: string
                            releaseFences:
                              description: releaseFences authorize removal of exact Pod UIDs from exact stable slots.
                              items:
                                description: EngineGroupReleaseAuthorization permits removal of only named concrete Pod incarnations.
                                properties:
                                  capacityRefs:
                                    description: capacityRefs is the complete set of concrete Pod UIDs authorized for deletion.
                                    items:
                                      description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                      properties:
                                        name:
                                          description: name is the Pod name.
                                          minLength: 1
                                          type: string
                                        uid:
                                          description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                          type: string
                                      required:
                                        - name
                                        - uid
                                      type: object
                                      x-kubernetes-validations:
                                        - message: uid must not be empty
                                          rule: size(self.uid) > 0
                                    minItems: 1
                                    type: array
                                  operationID:
                                    description: operationID identifies the membership result authorizing release.
                                    minLength: 1
                                    type: string
                                  replicaID:
                                    description: replicaID is the stable logical replica being released.
                                    minLength: 1
                                    type: string
                                  slotID:
                                    description: slotID is the stable workload-manager position being fenced.
                                    minLength: 1
                                    type: string
                                  topologyGeneration:
                                    description: topologyGeneration identifies the exact committed topology authorizing release.
                                    format: int64
                                    minimum: 1
                                    type: integer
                                required:
                                  - capacityRefs
                                  - operationID
                                  - replicaID
                                  - slotID
                                  - topologyGeneration
                                type: object
                              type: array
                            replicas:
                              description: replicas is the complete desired set of physical replica allocations.
                              items:
                                description: EngineGroupCapacityReplicaTargetStatus describes one allocation required by an absolute target.
                                properties:
                                  bootstrap:
                                    description: bootstrap authorizes creation of a new orchestrator-owned incarnation.
                                    properties:
                                      baseTopologyGeneration:
                                        description: baseTopologyGeneration binds bootstrap to the topology from which the change starts.
                                        format: int64
                                        minimum: 1
                                        type: integer
                                      mode:
                                        description: mode distinguishes fresh joining capacity from fixed-slot restoration.
                                        enum:
                                          - Join
                                          - RestoreFixedSlot
                                        type: string
                                      nativeMembers:
                                        description: nativeMembers names the fixed backend identities when the bootstrap mode requires them.
                                        items:
                                          minLength: 1
                                          type: string
                                        type: array
                                    required:
                                      - baseTopologyGeneration
                                      - mode
                                    type: object
                                  incarnation:
                                    description: incarnation asserts the exact existing capacity that must remain assigned.
                                    properties:
                                      capacityRefs:
                                        description: capacityRefs contains every concrete Pod in this replica allocation.
                                        items:
                                          description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                          properties:
                                            name:
                                              description: name is the Pod name.
                                              minLength: 1
                                              type: string
                                            uid:
                                              description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                              type: string
                                          required:
                                            - name
                                            - uid
                                          type: object
                                          x-kubernetes-validations:
                                            - message: uid must not be empty
                                              rule: size(self.uid) > 0
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process incarnation.
                                        minLength: 1
                                        type: string
                                      slotID:
                                        description: slotID is the stable workload-manager position.
                                        minLength: 1
                                        type: string
                                    required:
                                      - capacityRefs
                                      - replicaID
                                      - runtimeIncarnation
                                      - slotID
                                    type: object
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  slotID:
                                    description: slotID identifies the stable workload-manager position.
                                    minLength: 1
                                    type: string
                                required:
                                  - replicaID
                                  - slotID
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                                - replicaID
                              x-kubernetes-list-type: map
                            transitionID:
                              description: transitionID correlates this target with one membership transition.
                              minLength: 1
                              type: string
                          required:
                            - controlRevision
                            - processLifecycleOwner
                            - profileFingerprint
                            - transitionID
                          type: object
                        observed:
                          description: observed is the workload manager's authoritative allocation and release-fence state.
                          properties:
                            allocations:
                              description: allocations contains every concrete logical-replica allocation currently owned by the group.
                              items:
                                description: EngineGroupCapacityAllocationStatus is one complete physical allocation and its availability.
                                properties:
                                  available:
                                    description: available reports whether the complete allocation satisfies profile-required readiness.
                                    type: boolean
                                  incarnation:
                                    description: incarnation binds the allocation to stable and concrete identities.
                                    properties:
                                      capacityRefs:
                                        description: capacityRefs contains every concrete Pod in this replica allocation.
                                        items:
                                          description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                          properties:
                                            name:
                                              description: name is the Pod name.
                                              minLength: 1
                                              type: string
                                            uid:
                                              description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                              type: string
                                          required:
                                            - name
                                            - uid
                                          type: object
                                          x-kubernetes-validations:
                                            - message: uid must not be empty
                                              rule: size(self.uid) > 0
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process incarnation.
                                        minLength: 1
                                        type: string
                                      slotID:
                                        description: slotID is the stable workload-manager position.
                                        minLength: 1
                                        type: string
                                    required:
                                      - capacityRefs
                                      - replicaID
                                      - runtimeIncarnation
                                      - slotID
                                    type: object
                                  replicaID:
                                    description: replicaID duplicates incarnation.replicaID as the list-map key.
                                    minLength: 1
                                    type: string
                                required:
                                  - available
                                  - incarnation
                                  - replicaID
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                                - replicaID
                              x-kubernetes-list-type: map
                            appliedRevision:
                              description: appliedRevision is the last capacity target revision durably accepted by the adapter.
                              format: int64
                              minimum: 0
                              type: integer
                            releaseFences:
                              description: releaseFences are the exact release authorizations durably enforced by the adapter.
                              items:
                                description: EngineGroupReleaseAuthorization permits removal of only named concrete Pod incarnations.
                                properties:
                                  capacityRefs:
                                    description: capacityRefs is the complete set of concrete Pod UIDs authorized for deletion.
                                    items:
                                      description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                      properties:
                                        name:
                                          description: name is the Pod name.
                                          minLength: 1
                                          type: string
                                        uid:
                                          description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                          type: string
                                      required:
                                        - name
                                        - uid
                                      type: object
                                      x-kubernetes-validations:
                                        - message: uid must not be empty
                                          rule: size(self.uid) > 0
                                    minItems: 1
                                    type: array
                                  operationID:
                                    description: operationID identifies the membership result authorizing release.
                                    minLength: 1
                                    type: string
                                  replicaID:
                                    description: replicaID is the stable logical replica being released.
                                    minLength: 1
                                    type: string
                                  slotID:
                                    description: slotID is the stable workload-manager position being fenced.
                                    minLength: 1
                                    type: string
                                  topologyGeneration:
                                    description: topologyGeneration identifies the exact committed topology authorizing release.
                                    format: int64
                                    minimum: 1
                                    type: integer
                                required:
                                  - capacityRefs
                                  - operationID
                                  - replicaID
                                  - slotID
                                  - topologyGeneration
                                type: object
                              type: array
                          required:
                            - appliedRevision
                          type: object
                      required:
                        - observed
                      type: object
                    controlRevision:
                      description: controlRevision orders every capacity, traffic, and membership target within this group.
                      format: int64
                      minimum: 0
                      type: integer
                    membership:
                      description: membership records the exact desired transition and adapter-owned correlated observation.
                      properties:
                        desired:
                          description: desired is one exact immutable membership compare-and-apply target.
                          properties:
                            baseTopology:
                              description: baseTopology is the complete topology against which the target is atomically compared.
                              properties:
                                generation:
                                  description: generation is the engine's topology generation.
                                  format: int64
                                  minimum: 1
                                  type: integer
                                replicas:
                                  description: replicas is the complete logical-to-native membership mapping at this generation.
                                  items:
                                    description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                    properties:
                                      nativeMembers:
                                        description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                        items:
                                          minLength: 1
                                          type: string
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process in this topology.
                                        minLength: 1
                                        type: string
                                    required:
                                      - nativeMembers
                                      - replicaID
                                      - runtimeIncarnation
                                    type: object
                                  type: array
                                  x-kubernetes-list-map-keys:
                                    - replicaID
                                  x-kubernetes-list-type: map
                              required:
                                - generation
                              type: object
                            controlRevision:
                              description: controlRevision orders this target within the Engine Group.
                              format: int64
                              minimum: 1
                              type: integer
                            joining:
                              description: joining freezes exact runtime identities supplied to the membership transaction.
                              items:
                                description: EngineGroupJoiningReplicaStatus identifies one concrete process joining engine membership.
                                properties:
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  runtimeIncarnation:
                                    description: runtimeIncarnation identifies the concrete engine process.
                                    minLength: 1
                                    type: string
                                required:
                                  - replicaID
                                  - runtimeIncarnation
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                                - replicaID
                              x-kubernetes-list-type: map
                            plan:
                              description: plan is the immutable profile-resolved semantic change.
                              properties:
                                change:
                                  description: change is the serializable tagged union of exact membership semantics.
                                  properties:
                                    grow:
                                      description: grow adds previously unknown logical replicas.
                                      properties:
                                        replicas:
                                          description: replicas contains every new logical, slot, bootstrap, and native identity.
                                          items:
                                            description: EngineGroupReplicaTargetStatus is the resolved physical and native identity for one joining replica.
                                            properties:
                                              bootstrap:
                                                description: bootstrap distinguishes a new logical member from restoration of a fixed slot.
                                                enum:
                                                  - Join
                                                  - RestoreFixedSlot
                                                type: string
                                              nativeMembers:
                                                description: nativeMembers contains backend-native identities required by the resolved plan.
                                                items:
                                                  minLength: 1
                                                  type: string
                                                type: array
                                              replicaID:
                                                description: replicaID is the stable logical identity.
                                                minLength: 1
                                                type: string
                                              slotID:
                                                description: slotID is the stable workload-manager position.
                                                minLength: 1
                                                type: string
                                            required:
                                              - bootstrap
                                              - replicaID
                                              - slotID
                                            type: object
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-map-keys:
                                            - replicaID
                                          x-kubernetes-list-type: map
                                      required:
                                        - replicas
                                      type: object
                                    kind:
                                      description: kind selects the populated semantic variant.
                                      enum:
                                        - Grow
                                        - Retire
                                        - ReduceToSurvivors
                                        - Restore
                                        - Remap
                                      type: string
                                    reduceToSurvivors:
                                      description: reduceToSurvivors removes base members absent from an authoritative survivor set.
                                      properties:
                                        survivors:
                                          description: survivors is the exact authoritative survivor set.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-type: set
                                      required:
                                        - survivors
                                      type: object
                                    remap:
                                      description: remap changes complete native membership without changing logical cardinality.
                                      properties:
                                        membership:
                                          description: membership is the complete target logical, slot, and native-member mapping.
                                          items:
                                            description: EngineGroupNativeMembershipStatus is one stable logical-to-native membership mapping.
                                            properties:
                                              nativeMembers:
                                                description: nativeMembers is the complete backend-native identity set for the replica.
                                                items:
                                                  minLength: 1
                                                  type: string
                                                minItems: 1
                                                type: array
                                              replicaID:
                                                description: replicaID is the stable logical identity.
                                                minLength: 1
                                                type: string
                                              slotID:
                                                description: slotID is the stable workload-manager position.
                                                minLength: 1
                                                type: string
                                            required:
                                              - nativeMembers
                                              - replicaID
                                              - slotID
                                            type: object
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-map-keys:
                                            - replicaID
                                          x-kubernetes-list-type: map
                                      required:
                                        - membership
                                      type: object
                                    restore:
                                      description: restore recreates excluded stable identities in their original native slots.
                                      properties:
                                        replicas:
                                          description: replicas contains every exact stable identity to restore.
                                          items:
                                            description: EngineGroupReplicaTargetStatus is the resolved physical and native identity for one joining replica.
                                            properties:
                                              bootstrap:
                                                description: bootstrap distinguishes a new logical member from restoration of a fixed slot.
                                                enum:
                                                  - Join
                                                  - RestoreFixedSlot
                                                type: string
                                              nativeMembers:
                                                description: nativeMembers contains backend-native identities required by the resolved plan.
                                                items:
                                                  minLength: 1
                                                  type: string
                                                type: array
                                              replicaID:
                                                description: replicaID is the stable logical identity.
                                                minLength: 1
                                                type: string
                                              slotID:
                                                description: slotID is the stable workload-manager position.
                                                minLength: 1
                                                type: string
                                            required:
                                              - bootstrap
                                              - replicaID
                                              - slotID
                                            type: object
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-map-keys:
                                            - replicaID
                                          x-kubernetes-list-type: map
                                      required:
                                        - replicas
                                      type: object
                                    retire:
                                      description: retire gracefully removes selected healthy replicas.
                                      properties:
                                        replicas:
                                          description: replicas contains the exact stable logical identities selected for retirement.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-type: set
                                      required:
                                        - replicas
                                      type: object
                                  required:
                                    - kind
                                  type: object
                                  x-kubernetes-validations:
                                    - message: exactly one change variant must match kind
                                      rule: (self.kind == 'Grow' && has(self.grow) && !has(self.retire) && !has(self.reduceToSurvivors) && !has(self.restore) && !has(self.remap)) || (self.kind == 'Retire' && !has(self.grow) && has(self.retire) && !has(self.reduceToSurvivors) && !has(self.restore) && !has(self.remap)) || (self.kind == 'ReduceToSurvivors' && !has(self.grow) && !has(self.retire) && has(self.reduceToSurvivors) && !has(self.restore) && !has(self.remap)) || (self.kind == 'Restore' && !has(self.grow) && !has(self.retire) && !has(self.reduceToSurvivors) && has(self.restore) && !has(self.remap)) || (self.kind == 'Remap' && !has(self.grow) && !has(self.retire) && !has(self.reduceToSurvivors) && !has(self.restore) && has(self.remap))
                                id:
                                  description: id is the caller-selected stable plan identity.
                                  minLength: 1
                                  type: string
                                processLifecycleOwner:
                                  description: processLifecycleOwner identifies who starts and stops joining engine processes.
                                  enum:
                                    - Engine
                                    - Orchestrator
                                  type: string
                                profileFingerprint:
                                  description: profileFingerprint binds the plan to immutable engine and workload geometry.
                                  minLength: 1
                                  type: string
                                trafficRequirement:
                                  description: trafficRequirement declares whether retained members may serve during the mutation.
                                  enum:
                                    - KeepServing
                                    - QuiesceGroup
                                  type: string
                                verificationRequirement:
                                  description: verificationRequirement declares whether serving progress must be proven before admission.
                                  enum:
                                    - None
                                    - Required
                                  type: string
                              required:
                                - change
                                - id
                                - processLifecycleOwner
                                - profileFingerprint
                                - trafficRequirement
                                - verificationRequirement
                              type: object
                            targetDigest:
                              description: targetDigest is the shared-code canonical digest of this exact normalized target.
                              minLength: 1
                              type: string
                            transitionID:
                              description: transitionID uniquely identifies this target and its durable adapter transaction.
                              minLength: 1
                              type: string
                            validation:
                              description: validation is the durable adapter evidence for this exact target.
                              properties:
                                capabilityGeneration:
                                  description: capabilityGeneration identifies the adapter capability snapshot used for validation.
                                  minLength: 1
                                  type: string
                                planDigest:
                                  description: planDigest is the canonical digest of the normalized resolved plan.
                                  minLength: 1
                                  type: string
                                profileFingerprint:
                                  description: profileFingerprint binds validation to immutable engine and workload geometry.
                                  minLength: 1
                                  type: string
                                targetDigest:
                                  description: targetDigest is the canonical digest of the exact membership target, when known.
                                  type: string
                              required:
                                - capabilityGeneration
                                - planDigest
                                - profileFingerprint
                              type: object
                          required:
                            - baseTopology
                            - controlRevision
                            - plan
                            - targetDigest
                            - transitionID
                            - validation
                          type: object
                        observed:
                          description: observed contains authoritative committed topology and the correlated transition result.
                          properties:
                            committedTopology:
                              description: committedTopology is the engine's current authoritative complete topology.
                              properties:
                                generation:
                                  description: generation is the engine's topology generation.
                                  format: int64
                                  minimum: 1
                                  type: integer
                                replicas:
                                  description: replicas is the complete logical-to-native membership mapping at this generation.
                                  items:
                                    description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                    properties:
                                      nativeMembers:
                                        description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                        items:
                                          minLength: 1
                                          type: string
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process in this topology.
                                        minLength: 1
                                        type: string
                                    required:
                                      - nativeMembers
                                      - replicaID
                                      - runtimeIncarnation
                                    type: object
                                  type: array
                                  x-kubernetes-list-map-keys:
                                    - replicaID
                                  x-kubernetes-list-type: map
                              required:
                                - generation
                              type: object
                            requestedTransitionID:
                              description: requestedTransitionID records the exact identity requested from the adapter observer.
                              type: string
                            transition:
                              description: transition is the adapter's durable state for the requested transition; absence is authoritative.
                              properties:
                                controlRevision:
                                  description: controlRevision echoes the requested controller revision.
                                  format: int64
                                  minimum: 1
                                  type: integer
                                failure:
                                  description: failure describes an authoritative rejection or unknown outcome.
                                  properties:
                                    classification:
                                      description: classification states whether the same external intent may be retried.
                                      enum:
                                        - Retryable
                                        - Terminal
                                      type: string
                                    message:
                                      description: message explains the failure for a human reader.
                                      type: string
                                    reason:
                                      description: reason is a stable machine-readable failure reason.
                                      minLength: 1
                                      type: string
                                  required:
                                    - classification
                                    - reason
                                  type: object
                                phase:
                                  description: phase is the adapter-owned durable transaction state.
                                  enum:
                                    - Pending
                                    - Committed
                                    - Rejected
                                    - Unknown
                                  type: string
                                resultTopology:
                                  description: resultTopology is the immutable topology produced by a committed transition.
                                  properties:
                                    generation:
                                      description: generation is the engine's topology generation.
                                      format: int64
                                      minimum: 1
                                      type: integer
                                    replicas:
                                      description: replicas is the complete logical-to-native membership mapping at this generation.
                                      items:
                                        description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                        properties:
                                          nativeMembers:
                                            description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                            items:
                                              minLength: 1
                                              type: string
                                            minItems: 1
                                            type: array
                                          replicaID:
                                            description: replicaID is the stable logical identity.
                                            minLength: 1
                                            type: string
                                          runtimeIncarnation:
                                            description: runtimeIncarnation identifies the concrete engine process in this topology.
                                            minLength: 1
                                            type: string
                                        required:
                                          - nativeMembers
                                          - replicaID
                                          - runtimeIncarnation
                                        type: object
                                      type: array
                                      x-kubernetes-list-map-keys:
                                        - replicaID
                                      x-kubernetes-list-type: map
                                  required:
                                    - generation
                                  type: object
                                targetDigest:
                                  description: targetDigest echoes the requested canonical target digest.
                                  minLength: 1
                                  type: string
                                transitionID:
                                  description: transitionID echoes the requested transition identity.
                                  minLength: 1
                                  type: string
                              required:
                                - controlRevision
                                - phase
                                - targetDigest
                                - transitionID
                              type: object
                          required:
                            - committedTopology
                          type: object
                      required:
                        - observed
                      type: object
                    registry:
                      description: registry preserves stable logical-replica and capacity-slot identities across replacement.
                      items:
                        description: EngineGroupReplicaRecordStatus is the canonical durable record for one logical replica and stable slot.
                        properties:
                          current:
                            description: current is the concrete physical and runtime incarnation currently assigned to the slot.
                            properties:
                              capacityRefs:
                                description: capacityRefs contains every concrete Pod in this replica allocation.
                                items:
                                  description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                  properties:
                                    name:
                                      description: name is the Pod name.
                                      minLength: 1
                                      type: string
                                    uid:
                                      description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                      type: string
                                  required:
                                    - name
                                    - uid
                                  type: object
                                  x-kubernetes-validations:
                                    - message: uid must not be empty
                                      rule: size(self.uid) > 0
                                minItems: 1
                                type: array
                              replicaID:
                                description: replicaID is the stable logical identity.
                                minLength: 1
                                type: string
                              runtimeIncarnation:
                                description: runtimeIncarnation identifies the concrete engine process incarnation.
                                minLength: 1
                                type: string
                              slotID:
                                description: slotID is the stable workload-manager position.
                                minLength: 1
                                type: string
                            required:
                              - capacityRefs
                              - replicaID
                              - runtimeIncarnation
                              - slotID
                            type: object
                          history:
                            description: history retains excluded incarnations and their exact native membership.
                            items:
                              description: EngineGroupReplicaHistoryStatus retains one excluded incarnation and its exact native membership.
                              properties:
                                incarnation:
                                  description: incarnation is the concrete allocation and runtime identity that was excluded.
                                  properties:
                                    capacityRefs:
                                      description: capacityRefs contains every concrete Pod in this replica allocation.
                                      items:
                                        description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                        properties:
                                          name:
                                            description: name is the Pod name.
                                            minLength: 1
                                            type: string
                                          uid:
                                            description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                            type: string
                                        required:
                                          - name
                                          - uid
                                        type: object
                                        x-kubernetes-validations:
                                          - message: uid must not be empty
                                            rule: size(self.uid) > 0
                                      minItems: 1
                                      type: array
                                    replicaID:
                                      description: replicaID is the stable logical identity.
                                      minLength: 1
                                      type: string
                                    runtimeIncarnation:
                                      description: runtimeIncarnation identifies the concrete engine process incarnation.
                                      minLength: 1
                                      type: string
                                    slotID:
                                      description: slotID is the stable workload-manager position.
                                      minLength: 1
                                      type: string
                                  required:
                                    - capacityRefs
                                    - replicaID
                                    - runtimeIncarnation
                                    - slotID
                                  type: object
                                nativeMembers:
                                  description: nativeMembers are the backend-native identities formerly correlated with this replica.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                topologyGeneration:
                                  description: topologyGeneration identifies the topology that excluded this incarnation.
                                  format: int64
                                  minimum: 1
                                  type: integer
                              required:
                                - incarnation
                                - nativeMembers
                                - topologyGeneration
                              type: object
                            type: array
                          replicaID:
                            description: replicaID is stable across physical and runtime replacement.
                            minLength: 1
                            type: string
                          slotID:
                            description: slotID identifies the stable workload-manager position backing this replica.
                            minLength: 1
                            type: string
                        required:
                          - replicaID
                          - slotID
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                        - replicaID
                      x-kubernetes-list-type: map
                    topologyHistory:
                      description: topologyHistory retains the current topology and snapshots referenced by durable evidence.
                      properties:
                        currentGeneration:
                          description: currentGeneration identifies the engine's current authoritative topology snapshot.
                          format: int64
                          minimum: 1
                          type: integer
                        snapshots:
                          description: snapshots contains the current topology and any snapshot still referenced by controller state.
                          items:
                            description: EngineGroupTopologyStatus is one immutable engine-authoritative committed topology snapshot.
                            properties:
                              generation:
                                description: generation is the engine's topology generation.
                                format: int64
                                minimum: 1
                                type: integer
                              replicas:
                                description: replicas is the complete logical-to-native membership mapping at this generation.
                                items:
                                  description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                  properties:
                                    nativeMembers:
                                      description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                      items:
                                        minLength: 1
                                        type: string
                                      minItems: 1
                                      type: array
                                    replicaID:
                                      description: replicaID is the stable logical identity.
                                      minLength: 1
                                      type: string
                                    runtimeIncarnation:
                                      description: runtimeIncarnation identifies the concrete engine process in this topology.
                                      minLength: 1
                                      type: string
                                  required:
                                    - nativeMembers
                                    - replicaID
                                    - runtimeIncarnation
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                  - replicaID
                                x-kubernetes-list-type: map
                            required:
                              - generation
                            type: object
                          minItems: 1
                          type: array
                          x-kubernetes-list-map-keys:
                            - generation
                          x-kubernetes-list-type: map
                      required:
                        - currentGeneration
                        - snapshots
                      type: object
                    traffic:
                      description: traffic records the newest desired target, the last accepted target, and routing observation.
                      properties:
                        accepted:
                          description: accepted is the last exact target durably acknowledged by the traffic adapter.
                          properties:
                            admitted:
                              description: admitted is the exact set of member incarnations eligible for new work.
                              items:
                                description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                properties:
                                  nativeMembers:
                                    description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                    items:
                                      minLength: 1
                                      type: string
                                    minItems: 1
                                    type: array
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  runtimeIncarnation:
                                    description: runtimeIncarnation identifies the concrete engine process in this topology.
                                    minLength: 1
                                    type: string
                                required:
                                  - nativeMembers
                                  - replicaID
                                  - runtimeIncarnation
                                type: object
                              type: array
                            controlRevision:
                              description: controlRevision orders this target within the Engine Group.
                              format: int64
                              minimum: 1
                              type: integer
                            drain:
                              description: drain contains the exact terminal non-serving evidence requested for each member.
                              items:
                                description: EngineGroupTrafficDrainTargetStatus requests terminal non-serving evidence for one member.
                                properties:
                                  membership:
                                    description: membership is the exact member incarnation to withdraw and drain.
                                    properties:
                                      nativeMembers:
                                        description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                        items:
                                          minLength: 1
                                          type: string
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process in this topology.
                                        minLength: 1
                                        type: string
                                    required:
                                      - nativeMembers
                                      - replicaID
                                      - runtimeIncarnation
                                    type: object
                                  mode:
                                    description: mode distinguishes graceful drain from confirmation that a failed member is inactive.
                                    enum:
                                      - Graceful
                                      - ConfirmInactive
                                    type: string
                                required:
                                  - membership
                                  - mode
                                type: object
                              type: array
                            topologyGeneration:
                              description: topologyGeneration binds the target to one authoritative membership snapshot.
                              format: int64
                              minimum: 1
                              type: integer
                            transitionID:
                              description: transitionID correlates this target with one membership transition.
                              minLength: 1
                              type: string
                          required:
                            - controlRevision
                            - topologyGeneration
                            - transitionID
                          type: object
                        desired:
                          description: desired is the newest persisted absolute routing and drain target.
                          properties:
                            admitted:
                              description: admitted is the exact set of member incarnations eligible for new work.
                              items:
                                description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                properties:
                                  nativeMembers:
                                    description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                    items:
                                      minLength: 1
                                      type: string
                                    minItems: 1
                                    type: array
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  runtimeIncarnation:
                                    description: runtimeIncarnation identifies the concrete engine process in this topology.
                                    minLength: 1
                                    type: string
                                required:
                                  - nativeMembers
                                  - replicaID
                                  - runtimeIncarnation
                                type: object
                              type: array
                            controlRevision:
                              description: controlRevision orders this target within the Engine Group.
                              format: int64
                              minimum: 1
                              type: integer
                            drain:
                              description: drain contains the exact terminal non-serving evidence requested for each member.
                              items:
                                description: EngineGroupTrafficDrainTargetStatus requests terminal non-serving evidence for one member.
                                properties:
                                  membership:
                                    description: membership is the exact member incarnation to withdraw and drain.
                                    properties:
                                      nativeMembers:
                                        description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                        items:
                                          minLength: 1
                                          type: string
                                        minItems: 1
                                        type: array
                                      replicaID:
                                        description: replicaID is the stable logical identity.
                                        minLength: 1
                                        type: string
                                      runtimeIncarnation:
                                        description: runtimeIncarnation identifies the concrete engine process in this topology.
                                        minLength: 1
                                        type: string
                                    required:
                                      - nativeMembers
                                      - replicaID
                                      - runtimeIncarnation
                                    type: object
                                  mode:
                                    description: mode distinguishes graceful drain from confirmation that a failed member is inactive.
                                    enum:
                                      - Graceful
                                      - ConfirmInactive
                                    type: string
                                required:
                                  - membership
                                  - mode
                                type: object
                              type: array
                            topologyGeneration:
                              description: topologyGeneration binds the target to one authoritative membership snapshot.
                              format: int64
                              minimum: 1
                              type: integer
                            transitionID:
                              description: transitionID correlates this target with one membership transition.
                              minLength: 1
                              type: string
                          required:
                            - controlRevision
                            - topologyGeneration
                            - transitionID
                          type: object
                        observed:
                          description: observed is the runtime's authoritative routing and drain state.
                          properties:
                            admitted:
                              description: admitted is the exact set of member incarnations eligible for new work.
                              items:
                                description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                properties:
                                  nativeMembers:
                                    description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                    items:
                                      minLength: 1
                                      type: string
                                    minItems: 1
                                    type: array
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  runtimeIncarnation:
                                    description: runtimeIncarnation identifies the concrete engine process in this topology.
                                    minLength: 1
                                    type: string
                                required:
                                  - nativeMembers
                                  - replicaID
                                  - runtimeIncarnation
                                type: object
                              type: array
                            appliedRevision:
                              description: appliedRevision is the last traffic target revision durably accepted by the adapter.
                              format: int64
                              minimum: 0
                              type: integer
                            drained:
                              description: drained is durable terminal non-serving evidence for exact member incarnations.
                              items:
                                description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                properties:
                                  nativeMembers:
                                    description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                    items:
                                      minLength: 1
                                      type: string
                                    minItems: 1
                                    type: array
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  runtimeIncarnation:
                                    description: runtimeIncarnation identifies the concrete engine process in this topology.
                                    minLength: 1
                                    type: string
                                required:
                                  - nativeMembers
                                  - replicaID
                                  - runtimeIncarnation
                                type: object
                              type: array
                            draining:
                              description: draining contains member incarnations whose graceful drain has not completed.
                              items:
                                description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                                properties:
                                  nativeMembers:
                                    description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                                    items:
                                      minLength: 1
                                      type: string
                                    minItems: 1
                                    type: array
                                  replicaID:
                                    description: replicaID is the stable logical identity.
                                    minLength: 1
                                    type: string
                                  runtimeIncarnation:
                                    description: runtimeIncarnation identifies the concrete engine process in this topology.
                                    minLength: 1
                                    type: string
                                required:
                                  - nativeMembers
                                  - replicaID
                                  - runtimeIncarnation
                                type: object
                              type: array
                          required:
                            - appliedRevision
                          type: object
                      required:
                        - observed
                      type: object
                    transition:
                      description: transition owns the immutable plan and cross-subsystem progress for one active or terminal change.
                      properties:
                        failure:
                          description: failure describes the reason an otherwise durable transition is blocked or reverting.
                          properties:
                            classification:
                              description: classification states whether the same external intent may be retried.
                              enum:
                                - Retryable
                                - Terminal
                              type: string
                            message:
                              description: message explains the failure for a human reader.
                              type: string
                            reason:
                              description: reason is a stable machine-readable failure reason.
                              minLength: 1
                              type: string
                          required:
                            - classification
                            - reason
                          type: object
                        outcome:
                          description: outcome summarizes cross-subsystem workflow progress without duplicating adapter phases.
                          enum:
                            - Progressing
                            - Reverting
                            - RolledBack
                            - Blocked
                            - Completed
                          type: string
                        planPreflight:
                          description: planPreflight is durable adapter evidence captured before capacity or traffic prework.
                          properties:
                            controlRevision:
                              description: |-
                                controlRevision is the controller revision at which validation completed.
                                Plan preflight runs before the first external target and therefore uses revision zero.
                              format: int64
                              minimum: 0
                              type: integer
                            evidence:
                              description: evidence is authoritative approval for the validated subject.
                              properties:
                                capabilityGeneration:
                                  description: capabilityGeneration identifies the adapter capability snapshot used for validation.
                                  minLength: 1
                                  type: string
                                planDigest:
                                  description: planDigest is the canonical digest of the normalized resolved plan.
                                  minLength: 1
                                  type: string
                                profileFingerprint:
                                  description: profileFingerprint binds validation to immutable engine and workload geometry.
                                  minLength: 1
                                  type: string
                                targetDigest:
                                  description: targetDigest is the canonical digest of the exact membership target, when known.
                                  type: string
                              required:
                                - capabilityGeneration
                                - planDigest
                                - profileFingerprint
                              type: object
                            rejection:
                              description: rejection is an authoritative terminal rejection of the validated subject.
                              properties:
                                classification:
                                  description: classification states whether the same external intent may be retried.
                                  enum:
                                    - Retryable
                                    - Terminal
                                  type: string
                                message:
                                  description: message explains the failure for a human reader.
                                  type: string
                                reason:
                                  description: reason is a stable machine-readable failure reason.
                                  minLength: 1
                                  type: string
                              required:
                                - classification
                                - reason
                              type: object
                            subjectDigest:
                              description: subjectDigest identifies the exact normalized plan or target that was validated.
                              minLength: 1
                              type: string
                            transitionID:
                              description: transitionID correlates this validation with the immutable transition.
                              minLength: 1
                              type: string
                          required:
                            - controlRevision
                            - subjectDigest
                            - transitionID
                          type: object
                        spec:
                          description: spec is the immutable transition identity, base topology, and resolved plan.
                          properties:
                            baseTopologyGeneration:
                              description: baseTopologyGeneration is the exact topology from which the change starts.
                              format: int64
                              minimum: 1
                              type: integer
                            id:
                              description: id uniquely identifies this transition.
                              minLength: 1
                              type: string
                            plan:
                              description: plan is the complete normalized semantic operation.
                              properties:
                                change:
                                  description: change is the serializable tagged union of exact membership semantics.
                                  properties:
                                    grow:
                                      description: grow adds previously unknown logical replicas.
                                      properties:
                                        replicas:
                                          description: replicas contains every new logical, slot, bootstrap, and native identity.
                                          items:
                                            description: EngineGroupReplicaTargetStatus is the resolved physical and native identity for one joining replica.
                                            properties:
                                              bootstrap:
                                                description: bootstrap distinguishes a new logical member from restoration of a fixed slot.
                                                enum:
                                                  - Join
                                                  - RestoreFixedSlot
                                                type: string
                                              nativeMembers:
                                                description: nativeMembers contains backend-native identities required by the resolved plan.
                                                items:
                                                  minLength: 1
                                                  type: string
                                                type: array
                                              replicaID:
                                                description: replicaID is the stable logical identity.
                                                minLength: 1
                                                type: string
                                              slotID:
                                                description: slotID is the stable workload-manager position.
                                                minLength: 1
                                                type: string
                                            required:
                                              - bootstrap
                                              - replicaID
                                              - slotID
                                            type: object
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-map-keys:
                                            - replicaID
                                          x-kubernetes-list-type: map
                                      required:
                                        - replicas
                                      type: object
                                    kind:
                                      description: kind selects the populated semantic variant.
                                      enum:
                                        - Grow
                                        - Retire
                                        - ReduceToSurvivors
                                        - Restore
                                        - Remap
                                      type: string
                                    reduceToSurvivors:
                                      description: reduceToSurvivors removes base members absent from an authoritative survivor set.
                                      properties:
                                        survivors:
                                          description: survivors is the exact authoritative survivor set.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-type: set
                                      required:
                                        - survivors
                                      type: object
                                    remap:
                                      description: remap changes complete native membership without changing logical cardinality.
                                      properties:
                                        membership:
                                          description: membership is the complete target logical, slot, and native-member mapping.
                                          items:
                                            description: EngineGroupNativeMembershipStatus is one stable logical-to-native membership mapping.
                                            properties:
                                              nativeMembers:
                                                description: nativeMembers is the complete backend-native identity set for the replica.
                                                items:
                                                  minLength: 1
                                                  type: string
                                                minItems: 1
                                                type: array
                                              replicaID:
                                                description: replicaID is the stable logical identity.
                                                minLength: 1
                                                type: string
                                              slotID:
                                                description: slotID is the stable workload-manager position.
                                                minLength: 1
                                                type: string
                                            required:
                                              - nativeMembers
                                              - replicaID
                                              - slotID
                                            type: object
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-map-keys:
                                            - replicaID
                                          x-kubernetes-list-type: map
                                      required:
                                        - membership
                                      type: object
                                    restore:
                                      description: restore recreates excluded stable identities in their original native slots.
                                      properties:
                                        replicas:
                                          description: replicas contains every exact stable identity to restore.
                                          items:
                                            description: EngineGroupReplicaTargetStatus is the resolved physical and native identity for one joining replica.
                                            properties:
                                              bootstrap:
                                                description: bootstrap distinguishes a new logical member from restoration of a fixed slot.
                                                enum:
                                                  - Join
                                                  - RestoreFixedSlot
                                                type: string
                                              nativeMembers:
                                                description: nativeMembers contains backend-native identities required by the resolved plan.
                                                items:
                                                  minLength: 1
                                                  type: string
                                                type: array
                                              replicaID:
                                                description: replicaID is the stable logical identity.
                                                minLength: 1
                                                type: string
                                              slotID:
                                                description: slotID is the stable workload-manager position.
                                                minLength: 1
                                                type: string
                                            required:
                                              - bootstrap
                                              - replicaID
                                              - slotID
                                            type: object
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-map-keys:
                                            - replicaID
                                          x-kubernetes-list-type: map
                                      required:
                                        - replicas
                                      type: object
                                    retire:
                                      description: retire gracefully removes selected healthy replicas.
                                      properties:
                                        replicas:
                                          description: replicas contains the exact stable logical identities selected for retirement.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                          x-kubernetes-list-type: set
                                      required:
                                        - replicas
                                      type: object
                                  required:
                                    - kind
                                  type: object
                                  x-kubernetes-validations:
                                    - message: exactly one change variant must match kind
                                      rule: (self.kind == 'Grow' && has(self.grow) && !has(self.retire) && !has(self.reduceToSurvivors) && !has(self.restore) && !has(self.remap)) || (self.kind == 'Retire' && !has(self.grow) && has(self.retire) && !has(self.reduceToSurvivors) && !has(self.restore) && !has(self.remap)) || (self.kind == 'ReduceToSurvivors' && !has(self.grow) && !has(self.retire) && has(self.reduceToSurvivors) && !has(self.restore) && !has(self.remap)) || (self.kind == 'Restore' && !has(self.grow) && !has(self.retire) && !has(self.reduceToSurvivors) && has(self.restore) && !has(self.remap)) || (self.kind == 'Remap' && !has(self.grow) && !has(self.retire) && !has(self.reduceToSurvivors) && !has(self.restore) && has(self.remap))
                                id:
                                  description: id is the caller-selected stable plan identity.
                                  minLength: 1
                                  type: string
                                processLifecycleOwner:
                                  description: processLifecycleOwner identifies who starts and stops joining engine processes.
                                  enum:
                                    - Engine
                                    - Orchestrator
                                  type: string
                                profileFingerprint:
                                  description: profileFingerprint binds the plan to immutable engine and workload geometry.
                                  minLength: 1
                                  type: string
                                trafficRequirement:
                                  description: trafficRequirement declares whether retained members may serve during the mutation.
                                  enum:
                                    - KeepServing
                                    - QuiesceGroup
                                  type: string
                                verificationRequirement:
                                  description: verificationRequirement declares whether serving progress must be proven before admission.
                                  enum:
                                    - None
                                    - Required
                                  type: string
                              required:
                                - change
                                - id
                                - processLifecycleOwner
                                - profileFingerprint
                                - trafficRequirement
                                - verificationRequirement
                              type: object
                          required:
                            - baseTopologyGeneration
                            - id
                            - plan
                          type: object
                        startedAt:
                          description: startedAt is when the controller first persisted the immutable transition.
                          format: date-time
                          type: string
                        targetPreflight:
                          description: targetPreflight is durable adapter evidence for the exact frozen membership target.
                          properties:
                            controlRevision:
                              description: |-
                                controlRevision is the controller revision at which validation completed.
                                Plan preflight runs before the first external target and therefore uses revision zero.
                              format: int64
                              minimum: 0
                              type: integer
                            evidence:
                              description: evidence is authoritative approval for the validated subject.
                              properties:
                                capabilityGeneration:
                                  description: capabilityGeneration identifies the adapter capability snapshot used for validation.
                                  minLength: 1
                                  type: string
                                planDigest:
                                  description: planDigest is the canonical digest of the normalized resolved plan.
                                  minLength: 1
                                  type: string
                                profileFingerprint:
                                  description: profileFingerprint binds validation to immutable engine and workload geometry.
                                  minLength: 1
                                  type: string
                                targetDigest:
                                  description: targetDigest is the canonical digest of the exact membership target, when known.
                                  type: string
                              required:
                                - capabilityGeneration
                                - planDigest
                                - profileFingerprint
                              type: object
                            rejection:
                              description: rejection is an authoritative terminal rejection of the validated subject.
                              properties:
                                classification:
                                  description: classification states whether the same external intent may be retried.
                                  enum:
                                    - Retryable
                                    - Terminal
                                  type: string
                                message:
                                  description: message explains the failure for a human reader.
                                  type: string
                                reason:
                                  description: reason is a stable machine-readable failure reason.
                                  minLength: 1
                                  type: string
                              required:
                                - classification
                                - reason
                              type: object
                            subjectDigest:
                              description: subjectDigest identifies the exact normalized plan or target that was validated.
                              minLength: 1
                              type: string
                            transitionID:
                              description: transitionID correlates this validation with the immutable transition.
                              minLength: 1
                              type: string
                          required:
                            - controlRevision
                            - subjectDigest
                            - transitionID
                          type: object
                        updatedAt:
                          description: updatedAt is when the controller last changed durable transition state.
                          format: date-time
                          type: string
                        verification:
                          description: verification records the topology-bound serving proof or conclusive failure.
                          properties:
                            failure:
                              description: failure is a conclusive verification failure.
                              properties:
                                classification:
                                  description: classification states whether the same external intent may be retried.
                                  enum:
                                    - Retryable
                                    - Terminal
                                  type: string
                                message:
                                  description: message explains the failure for a human reader.
                                  type: string
                                reason:
                                  description: reason is a stable machine-readable failure reason.
                                  minLength: 1
                                  type: string
                              required:
                                - classification
                                - reason
                              type: object
                            phase:
                              description: phase is the durable serving-verification state.
                              enum:
                                - Pending
                                - Passed
                                - Failed
                              type: string
                            proof:
                              description: proof is a positive result bound to one immutable topology.
                              properties:
                                observedAt:
                                  description: observedAt records when serving progress was proven.
                                  format: date-time
                                  type: string
                                runtimeDigest:
                                  description: runtimeDigest identifies the runtime-observed membership and serving path.
                                  minLength: 1
                                  type: string
                                topologyGeneration:
                                  description: topologyGeneration identifies the verified topology.
                                  format: int64
                                  minimum: 1
                                  type: integer
                              required:
                                - observedAt
                                - runtimeDigest
                                - topologyGeneration
                              type: object
                          required:
                            - phase
                          type: object
                      required:
                        - outcome
                        - spec
                        - startedAt
                        - updatedAt
                      type: object
                  required:
                    - capacity
                    - controlRevision
                    - membership
                    - topologyHistory
                    - traffic
                  type: object
                releaseAuthorizations:
                  description: releaseAuthorizations names the exact Pod UIDs that may be removed from stable replica slots.
                  items:
                    description: EngineGroupReleaseAuthorization permits removal of only named concrete Pod incarnations.
                    properties:
                      capacityRefs:
                        description: capacityRefs is the complete set of concrete Pod UIDs authorized for deletion.
                        items:
                          description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                          properties:
                            name:
                              description: name is the Pod name.
                              minLength: 1
                              type: string
                            uid:
                              description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                              type: string
                          required:
                            - name
                            - uid
                          type: object
                          x-kubernetes-validations:
                            - message: uid must not be empty
                              rule: size(self.uid) > 0
                        minItems: 1
                        type: array
                      operationID:
                        description: operationID identifies the membership result authorizing release.
                        minLength: 1
                        type: string
                      replicaID:
                        description: replicaID is the stable logical replica being released.
                        minLength: 1
                        type: string
                      slotID:
                        description: slotID is the stable workload-manager position being fenced.
                        minLength: 1
                        type: string
                      topologyGeneration:
                        description: topologyGeneration identifies the exact committed topology authorizing release.
                        format: int64
                        minimum: 1
                        type: integer
                    required:
                      - capacityRefs
                      - operationID
                      - replicaID
                      - slotID
                      - topologyGeneration
                    type: object
                  type: array
                replicaStates:
                  description: |-
                    replicaStates contains the stable identity and independently observed physical and engine
                    state of each known logical replica.
                  items:
                    description: EngineGroupReplicaStatus preserves one stable logical identity across physical replacement.
                    properties:
                      availability:
                        description: availability reports complete physical and profile-required runtime readiness.
                        enum:
                          - Available
                          - Unavailable
                          - Unknown
                        type: string
                      current:
                        description: current is the currently allocated physical and runtime incarnation.
                        properties:
                          capacityRefs:
                            description: capacityRefs contains every concrete Pod incarnation in this replica allocation.
                            items:
                              description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                              properties:
                                name:
                                  description: name is the Pod name.
                                  minLength: 1
                                  type: string
                                uid:
                                  description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                  type: string
                              required:
                                - name
                                - uid
                              type: object
                              x-kubernetes-validations:
                                - message: uid must not be empty
                                  rule: size(self.uid) > 0
                            minItems: 1
                            type: array
                          runtimeIncarnation:
                            description: runtimeIncarnation identifies one concrete engine process incarnation.
                            minLength: 1
                            type: string
                        required:
                          - capacityRefs
                          - runtimeIncarnation
                        type: object
                      membership:
                        description: membership reports committed engine state or current orchestration intent.
                        enum:
                          - Active
                          - Masked
                          - Joining
                          - Retiring
                          - Unknown
                        type: string
                      nativeMembers:
                        description: nativeMembers are the engine identities currently correlated with this logical replica.
                        items:
                          minLength: 1
                          type: string
                        type: array
                      previousIncarnations:
                        description: previousIncarnations retain identities required for recovery or exact release.
                        items:
                          description: EngineGroupReplicaIncarnation binds one logical replica and stable slot to concrete capacity.
                          properties:
                            capacityRefs:
                              description: capacityRefs contains every concrete Pod incarnation in this replica allocation.
                              items:
                                description: EngineGroupCapacityRef identifies one concrete Pod allocated to a logical replica.
                                properties:
                                  name:
                                    description: name is the Pod name.
                                    minLength: 1
                                    type: string
                                  uid:
                                    description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                                    type: string
                                required:
                                  - name
                                  - uid
                                type: object
                                x-kubernetes-validations:
                                  - message: uid must not be empty
                                    rule: size(self.uid) > 0
                              minItems: 1
                              type: array
                            runtimeIncarnation:
                              description: runtimeIncarnation identifies one concrete engine process incarnation.
                              minLength: 1
                              type: string
                          required:
                            - capacityRefs
                            - runtimeIncarnation
                          type: object
                        type: array
                      replicaID:
                        description: replicaID is stable across physical and runtime replacement.
                        minLength: 1
                        type: string
                      representativeRef:
                        description: representativeRef identifies the Pod selected by status.selector for this allocation.
                        properties:
                          name:
                            description: name is the Pod name.
                            minLength: 1
                            type: string
                          uid:
                            description: uid is the concrete Pod incarnation and prevents name reuse from inheriting authority.
                            type: string
                        required:
                          - name
                          - uid
                        type: object
                        x-kubernetes-validations:
                          - message: uid must not be empty
                            rule: size(self.uid) > 0
                      slotID:
                        description: slotID identifies the stable workload-manager position backing this replica.
                        minLength: 1
                        type: string
                    required:
                      - availability
                      - membership
                      - replicaID
                      - slotID
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - replicaID
                  x-kubernetes-list-type: map
                replicas:
                  description: |-
                    replicas is the number of logical replicas with complete physical allocations. It is the
                    current replica count exposed through the scale subresource.
                  format: int32
                  minimum: 0
                  type: integer
                scaleUnit:
                  description: scaleUnit names the logical unit counted by spec.replicas and status.replicas.
                  enum:
                    - replicas
                  type: string
                selector:
                  description: |-
                    selector matches exactly one representative Pod for every allocated logical replica.
                    It represents allocation, not availability or engine admission.
                  type: string
                targetValidation:
                  description: |-
                    targetValidation describes a desired replica target rejected from reconciliation-time
                    profile or capability information.
                  properties:
                    effectiveReplicas:
                      description: effectiveReplicas is the last valid target still in force.
                      format: int32
                      minimum: 0
                      type: integer
                    maxReplicas:
                      description: maxReplicas is the effective upper bound used for validation.
                      format: int32
                      minimum: 1
                      type: integer
                    message:
                      description: message explains the rejection for a human reader.
                      type: string
                    minReplicas:
                      description: minReplicas is the effective lower bound used for validation.
                      format: int32
                      minimum: 1
                      type: integer
                    reason:
                      description: reason is a stable machine-readable rejection reason.
                      minLength: 1
                      type: string
                    requestedReplicas:
                      description: requestedReplicas is the rejected desired target.
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                    - effectiveReplicas
                    - reason
                    - requestedReplicas
                  type: object
                topology:
                  description: topology is the engine's current authoritative committed topology.
                  properties:
                    generation:
                      description: generation is the engine's topology generation.
                      format: int64
                      minimum: 1
                      type: integer
                    replicas:
                      description: replicas is the complete logical-to-native membership mapping at this generation.
                      items:
                        description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                        properties:
                          nativeMembers:
                            description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                            items:
                              minLength: 1
                              type: string
                            minItems: 1
                            type: array
                          replicaID:
                            description: replicaID is the stable logical identity.
                            minLength: 1
                            type: string
                          runtimeIncarnation:
                            description: runtimeIncarnation identifies the concrete engine process in this topology.
                            minLength: 1
                            type: string
                        required:
                          - nativeMembers
                          - replicaID
                          - runtimeIncarnation
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                        - replicaID
                      x-kubernetes-list-type: map
                  required:
                    - generation
                  type: object
                traffic:
                  description: traffic is the runtime's authoritative routing and drain observation.
                  properties:
                    admitted:
                      description: admitted is the exact set of member incarnations eligible for new work.
                      items:
                        description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                        properties:
                          nativeMembers:
                            description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                            items:
                              minLength: 1
                              type: string
                            minItems: 1
                            type: array
                          replicaID:
                            description: replicaID is the stable logical identity.
                            minLength: 1
                            type: string
                          runtimeIncarnation:
                            description: runtimeIncarnation identifies the concrete engine process in this topology.
                            minLength: 1
                            type: string
                        required:
                          - nativeMembers
                          - replicaID
                          - runtimeIncarnation
                        type: object
                      type: array
                    drained:
                      description: drained is durable terminal non-serving evidence for exact member incarnations.
                      items:
                        description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                        properties:
                          nativeMembers:
                            description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                            items:
                              minLength: 1
                              type: string
                            minItems: 1
                            type: array
                          replicaID:
                            description: replicaID is the stable logical identity.
                            minLength: 1
                            type: string
                          runtimeIncarnation:
                            description: runtimeIncarnation identifies the concrete engine process in this topology.
                            minLength: 1
                            type: string
                        required:
                          - nativeMembers
                          - replicaID
                          - runtimeIncarnation
                        type: object
                      type: array
                    draining:
                      description: draining is the exact set of member incarnations whose in-flight work has not completed.
                      items:
                        description: EngineGroupMemberStatus is one engine-owned logical, runtime, and native-member mapping.
                        properties:
                          nativeMembers:
                            description: nativeMembers are the backend-specific ranks or member identities committed for the replica.
                            items:
                              minLength: 1
                              type: string
                            minItems: 1
                            type: array
                          replicaID:
                            description: replicaID is the stable logical identity.
                            minLength: 1
                            type: string
                          runtimeIncarnation:
                            description: runtimeIncarnation identifies the concrete engine process in this topology.
                            minLength: 1
                            type: string
                        required:
                          - nativeMembers
                          - replicaID
                          - runtimeIncarnation
                        type: object
                      type: array
                    operationID:
                      description: operationID identifies the operation for which this observation was produced.
                      type: string
                    topologyGeneration:
                      description: topologyGeneration binds the observation to an exact committed topology.
                      format: int64
                      minimum: 1
                      type: integer
                  required:
                    - topologyGeneration
                  type: object
              required:
                - replicas
              type: object
          type: object
      served: true
      storage: true
      subresources:
        scale:
          labelSelectorPath: .status.selector
          specReplicasPath: .spec.replicas
          statusReplicasPath: .status.replicas
        status: {}
