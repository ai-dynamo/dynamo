# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Minimal DisaggregatedSet CRD used by envtest. The real schema is owned by
# the upstream LWS project; this fixture declares only the fields the operator
# reads and writes during controller tests.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: disaggregatedsets.disaggregatedset.x-k8s.io
spec:
  group: disaggregatedset.x-k8s.io
  scope: Namespaced
  names:
    plural: disaggregatedsets
    singular: disaggregatedset
    kind: DisaggregatedSet
    listKind: DisaggregatedSetList
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
