apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "compoundai-operator.fullname" . }}-compoundainimdeployment-editor-role
  labels:
  {{- include "compoundai-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - nvidia.com
  resources:
  - compoundainimdeployments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - nvidia.com
  resources:
  - compoundainimdeployments/status
  verbs:
  - get