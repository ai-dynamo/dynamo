# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# DeepSeek-V4.1-Flash on vLLM, disaggregated. Two prefill workers and one
# decode worker, TP4 each, 12x B200. Up to 1M tokens of context.
#
# Both pools carry the aggregated recipe's full flag set. What differs is the
# prefill batch budget, the long-prefill cap, the decode CUDA-graph ceiling,
# and the NIXL transport below.
#
# 2 prefill to 1 decode is balanced for this workload, not a free parameter.
# Both sides saturate together at the ship point: the prefill queue grows into
# TTFT, which still holds headroom, while decode ITL grows into the per-user
# token rate, which is what binds. Every other ratio measured worse or the same
# per GPU; 3P1D and 4P2D move the absolute number, not the number per GPU.
#
# KV rides NIXL over UCX on InfiniBand. Each worker requests an RDMA device.
# WITHOUT ONE, UCX falls back to TCP on eth0 silently -- no error, the
# deployment reports Ready -- and time to first token degrades by roughly an
# order of magnitude. The `rdma/ib` resource name is what the reference cluster
# advertises; other providers expose RDMA differently, so this is the field to
# retarget first. See README.md.
#
# Do not set --attention-backend, --moe-backend beyond what is here, or
# --linear-backend. vLLM resolves the rest for this model, and a mistyped
# --attention-backend value does not error: it silently falls through to a
# slower fallback. Do not set --block-size either; it resolves to 128 for this
# backend, and the DeepSeek-V4-Flash recipes' 256 belongs to a different model.
apiVersion: nvidia.com/v1beta1
kind: DynamoGraphDeployment
metadata:
  name: dsv41-flash-vllm-b200-disagg
spec:
  backendFramework: vllm
  components:
    - name: Frontend
      type: frontend
      replicas: 1
      podTemplate:
        spec:
          nodeSelector:
            kubernetes.io/arch: amd64
          tolerations:
            - key: kubernetes.io/arch
              operator: Exists
              effect: NoSchedule
          containers:
            - name: main
              image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.6.0-deepseek-v4.1-flash-dev.1
              imagePullPolicy: IfNotPresent
              command: [python3, -m, dynamo.frontend]
              args:
                - --http-port
                - "8000"
              env:
                - name: HF_HUB_OFFLINE
                  value: "1"
                - name: TRANSFORMERS_OFFLINE
                  value: "1"
                # The router scores workers from the KV events the workers
                # publish. See --kv-events-config on the workers.
                - name: DYN_ROUTER_MODE
                  value: kv
                # Conditional disaggregation stays OFF, and it is left unset
                # rather than set to "false". Removal is the documented default.
                # The valve keys on KV cache overlap, so it diverts the CACHED
                # requests straight to decode; measured here it cost 36 to 50
                # percent of throughput, because decode has no spare compute.
              ports:
                - name: http
                  containerPort: 8000
                  protocol: TCP
              resources:
                requests:
                  cpu: "4"
                  memory: 8Gi
    - name: PrefillWorker
      type: worker
      replicas: 2
      sharedMemorySize: 200Gi
      podTemplate:
        spec:
          nodeSelector:
            nvidia.com/gpu.product: NVIDIA-B200
          tolerations: &workerTolerations
            - key: nvidia.com/gpu
              operator: Exists
              effect: NoSchedule
            - key: kubernetes.io/arch
              operator: Exists
              effect: NoSchedule
          terminationGracePeriodSeconds: 60
          containers:
            - name: main
              image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.6.0-deepseek-v4.1-flash-dev.1
              imagePullPolicy: IfNotPresent
              command: [python3, -m, dynamo.vllm]
              args:
                - --disaggregation-mode
                - prefill
                # num_threads bounds how many KV transfers can be outstanding
                # before they queue behind the UCX worker threads. 4 measured
                # better than 8 at this ratio. The answer inverts at 1P1D, so
                # re-measure it if the ratio changes.
                - --kv-transfer-config
                - '{"kv_connector":"NixlConnector","kv_role":"kv_both","kv_buffer_device":"cuda","kv_connector_extra_config":{"backends":["UCX"],"num_threads":4}}'
                - --model
                - deepseek-ai/DeepSeek-V4.1-Flash
                - --served-model-name
                - deepseek-ai/DeepSeek-V4.1-Flash
                - --tokenizer-mode
                - deepseek_v41
                - --tensor-parallel-size
                - "4"
                # Expert parallelism gates the MegaMoE backend below: every
                # member of that family refuses to load without it.
                - --enable-expert-parallel
                - --moe-backend
                - deep_gemm_mega_moe
                # Load-time only. Each rank reads just its own expert shard from
                # disk, which shortens model load on a 510 GB checkpoint.
                - --enable-ep-weight-filter
                # Expert-parallel load balancing. The communicator is named
                # explicitly: it defaults to None, which prefers NIXL.
                - --enable-eplb
                - --eplb-config
                - '{"communicator":"torch_gloo"}'
                # The engram tables are pinned host memory, about 47 GiB per
                # rank, read over UVA on every token, so NUMA placement matters.
                - --numa-bind
                # The checkpoint accepts images. This recipe serves text, so
                # drop the vision tower and its encoder cache.
                - --language-model-only
                # Two reasoning parsers, and both are required. The engine-side
                # one gates guided decoding; the Dynamo-side one splits the
                # response into reasoning_content and content. Do not also set
                # the engine-side --tool-call-parser: Dynamo refuses both halves
                # of the tool-call pair at once.
                - --reasoning-parser
                - deepseek_v41
                - --dyn-reasoning-parser
                - deepseek_v41
                - --dyn-tool-call-parser
                - deepseek_v41
                # Without this, response_format is accepted but never enforced.
                - --dyn-enable-structural-tag
                # enable_kv_cache_events is load-bearing. Omit it and it
                # defaults to false and DYN_ROUTER_MODE=kv silently degrades to
                # load-based routing.
                - --kv-events-config
                - '{"publisher":"zmq","topic":"kv-events","endpoint":"tcp://*:5557","enable_kv_cache_events":true}'
                # PREFILL ONLY. An agentic input median near 67,000 tokens
                # against the 16,384 default means one request's chunk fills the
                # batch, so the pool runs one request at a time and serializes.
                - --max-num-batched-tokens
                - "32768"
                # PREFILL ONLY. Defaults to 0, which disables it, so one giant
                # request takes the whole batch budget for a step and starves
                # the queue behind it. This caps only the rows past the
                # threshold; queued requests still fill the rest of the step, so
                # GEMM efficiency holds. It redistributes wait rather than
                # removing it: the giants slow down and everything behind them
                # speeds up, so the TTFT tail pays for the median.
                - --long-prefill-token-threshold
                - "4096"
                - --max-cudagraph-capture-size
                - "512"
                # The single largest lever on this workload. The resolved
                # default is 0, which retains only a 160-token tail at the
                # previous prompt end, and the reconciled prefix hit is the
                # minimum across groups, so that tail caps every request.
                - --prefix-cache-retention-interval
                - "1024"
                # The DeepSeek sparse indexer K cache, fp8 to mxfp4. "auto"
                # resolves to fp8, so fp8 is the baseline, not bf16. mxfp4
                # requires compute capability 10.0; GB200 and B200 both qualify.
                - --attention-config
                - '{"indexer_kv_dtype":"mxfp4"}'
                # DSpark. The checkpoint's own dspark_block_size is 5; depth 3
                # measured better here. Adaptive verification is off: it is the
                # established cause of a CUDA illegal memory access on this
                # model.
                - --speculative-config
                - '{"method":"dspark","num_speculative_tokens":3,"draft_sample_method":"probabilistic","rejection_sample_method":"block","enable_adaptive_verification":false}'
              env: &workerEnv
                - name: HF_HOME
                  value: /shared-model-cache
                - name: HF_HUB_OFFLINE
                  value: "1"
                # tcp MUST stay in the list. cuda_ipc and cuda_copy are
                # memory-type transports only and cannot carry NIXL's UCX
                # active-message CONTROL plane. tcp here is the control plane,
                # not a data fallback; removing it breaks the handshake. rc_x is
                # first because it is the accelerated-verbs RC path, and rc is
                # what actually carries KV over RDMA.
                - name: UCX_TLS
                  value: rc_x,rc,cuda_copy,cuda_ipc,tcp
                # Always rendezvous, so every KV descriptor is zero-copy RDMA.
                # The descriptors here are small enough that UCX's default
                # threshold can place them on the eager path, which copies
                # through bounce buffers.
                - name: UCX_RNDV_SCHEME
                  value: get_zcopy
                - name: UCX_RNDV_THRESH
                  value: "0"
                - name: UCX_RC_TIMEOUT
                  value: 600s
                - name: UCX_KEEPALIVE_INTERVAL
                  value: 300s
                # NVLink stops at the node boundary on B200 and IB carries every
                # cross-node hop, so there is no multi-node NVLink domain to
                # enable. The GB200 recipe sets this to "y".
                - name: UCX_CUDA_IPC_ENABLE_MNNVL
                  value: "n"
                - name: UCX_MEMTYPE_CACHE
                  value: "y"
                # UCX_IB_GID_INDEX is deliberately NOT set. GID index 3 is the
                # RoCEv2 convention; these ports report link_layer InfiniBand,
                # so it names a scheme that does not apply here. The same goes
                # for UCX_IB_ADDR_TYPE=eth.
                - name: TRANSFORMERS_OFFLINE
                  value: "1"
                # The default is 600 s. This checkpoint is 510 GB and engine
                # init alone measures over 600 s, after a silent engram load.
                - name: VLLM_ENGINE_READY_TIMEOUT_S
                  value: "3600"
                # Dynamo marks an endpoint notready when its canary health check
                # times out, and /live then returns 503, which the kubelet kills
                # on. The default is 3 seconds, and the canary queues behind
                # real work.
                - name: DYN_HEALTH_CHECK_REQUEST_TIMEOUT
                  value: "300"
                # A corrected default, not an optimization. The
                # adaptive-verification cost curve profiles at 8192 tokens of KV
                # by default, against an agentic input median near 67,000.
                - name: VLLM_ADAPTIVE_VERIFICATION_PROFILE_CONTEXT_LEN
                  value: "65536"
              ports: &workerPorts
                - name: system
                  containerPort: 9090
                  protocol: TCP
                - name: kv-events
                  containerPort: 5557
                  protocol: TCP
              # The operator default is failureThreshold 1 with a 4 s timeout,
              # so one failed probe restarts the worker. The failure mode here
              # is a 503 rather than a timeout: the endpoint answers and reports
              # not-live while the scheduler is inside a forward pass.
              livenessProbe: &workerLiveness
                httpGet: {path: /live, port: system}
                periodSeconds: 10
                timeoutSeconds: 10
                failureThreshold: 60
              readinessProbe: &workerReadiness
                httpGet: {path: /health, port: system}
                periodSeconds: 10
                timeoutSeconds: 10
                failureThreshold: 60
              # Separate on purpose: first boot measures about 46 minutes to
              # ready on this SKU, dominated by a silent engram read with the
              # GPUs at 0 percent.
              startupProbe: &workerStartup
                httpGet: {path: /health, port: system}
                periodSeconds: 30
                timeoutSeconds: 10
                failureThreshold: 120
              resources: &workerResources
                requests:
                  cpu: "8"
                  # Must clear the pinned engram host memory, about 189 GiB per
                  # node at TP4, with real headroom. No limit is set on purpose:
                  # a hard limit turns a pinned-allocation spike into an
                  # OOMKill rather than reclaim.
                  memory: 512Gi
                  ephemeral-storage: 16Gi
                  nvidia.com/gpu: "4"
                  rdma/ib: "4"
                limits:
                  nvidia.com/gpu: "4"
                  rdma/ib: "4"
              # Workers run as root because FlashInfer's TRT-LLM FP4 MoE JIT
              # writes cubins into a root-owned site-packages directory during
              # determine_available_memory.
              securityContext: &workerSecurityContext
                runAsUser: 0
                runAsGroup: 0
                capabilities:
                  add: [IPC_LOCK, SYS_PTRACE, SYS_RESOURCE]
              volumeMounts: &workerVolumeMounts
                - name: shared-model-cache
                  mountPath: /shared-model-cache
          volumes: &workerVolumes
            - name: shared-model-cache
              persistentVolumeClaim:
                claimName: shared-model-cache
    - name: DecodeWorker
      type: worker
      replicas: 1
      sharedMemorySize: 200Gi
      podTemplate:
        spec:
          nodeSelector:
            nvidia.com/gpu.product: NVIDIA-B200
          tolerations: *workerTolerations
          terminationGracePeriodSeconds: 60
          containers:
            - name: main
              image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.6.0-deepseek-v4.1-flash-dev.1
              imagePullPolicy: IfNotPresent
              command: [python3, -m, dynamo.vllm]
              args:
                - --disaggregation-mode
                - decode
                - --kv-transfer-config
                - '{"kv_connector":"NixlConnector","kv_role":"kv_both","kv_buffer_device":"cuda","kv_connector_extra_config":{"backends":["UCX"],"num_threads":4}}'
                - --model
                - deepseek-ai/DeepSeek-V4.1-Flash
                - --served-model-name
                - deepseek-ai/DeepSeek-V4.1-Flash
                - --tokenizer-mode
                - deepseek_v41
                - --tensor-parallel-size
                - "4"
                - --enable-expert-parallel
                - --moe-backend
                - deep_gemm_mega_moe
                - --enable-ep-weight-filter
                - --enable-eplb
                - --eplb-config
                - '{"communicator":"torch_gloo"}'
                - --numa-bind
                - --language-model-only
                - --reasoning-parser
                - deepseek_v41
                - --dyn-reasoning-parser
                - deepseek_v41
                - --dyn-tool-call-parser
                - deepseek_v41
                - --dyn-enable-structural-tag
                - --kv-events-config
                - '{"publisher":"zmq","topic":"kv-events","endpoint":"tcp://*:5557","enable_kv_cache_events":true}'
                # DECODE ONLY, and higher than the prefill pool's 512. At 512
                # the decode batch is capped at 128 sequences, which this pool
                # exceeds at the ship concurrency.
                - --max-cudagraph-capture-size
                - "1024"
                - --prefix-cache-retention-interval
                - "1024"
                - --attention-config
                - '{"indexer_kv_dtype":"mxfp4"}'
                # Identical to the prefill pool. The draft head state and KV
                # layout must match across the NIXL handoff.
                - --speculative-config
                - '{"method":"dspark","num_speculative_tokens":3,"draft_sample_method":"probabilistic","rejection_sample_method":"block","enable_adaptive_verification":false}'
              env: *workerEnv
              ports: *workerPorts
              livenessProbe: *workerLiveness
              readinessProbe: *workerReadiness
              startupProbe: *workerStartup
              resources: *workerResources
              securityContext: *workerSecurityContext
              volumeMounts: *workerVolumeMounts
          volumes: *workerVolumes
