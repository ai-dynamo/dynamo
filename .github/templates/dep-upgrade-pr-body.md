## Automated dependency upgrade — $FW v$VER

This PR is generated by the Auto Dependency Upgrade pipeline. Post-merge CI passed on this branch before the PR was opened.

- **Framework:** $FW
- **Target version:** v$VER
- **Branch:** $BR
- **Validating run:** $RUN_URL

### Reviewer checklist
- [ ] Skim the upstream release notes for the target version.
- [ ] Confirm no torch-ecosystem versions need a coordinated bump (NGC base image is the source of truth for torch / triton / torchao).
- [ ] Mark the PR ready when satisfied — pre-merge CI runs once it leaves draft.

### Files modified
- `container/context.yaml` (`pip_wheel`, `github_trtllm_commit`)
- `pyproject.toml` (`tensorrt-llm==…` in `[project.optional-dependencies] trtllm`)
- `docs/reference/support-matrix.md` (`**main (ToT)**` row only)
