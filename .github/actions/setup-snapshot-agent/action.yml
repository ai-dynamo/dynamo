name: 'Setup Snapshot Agent'
description: 'Install the Dynamo snapshot-agent Helm chart in a vCluster namespace'

inputs:
  kubeconfig_base64:
    description: 'Base64-encoded kubeconfig for the vCluster'
    required: true
  namespace:
    description: 'Kubernetes namespace for the snapshot-agent'
    required: false
    default: default
  release_name:
    description: 'Helm release name'
    required: false
    default: snapshot
  snapshot_agent_image:
    description: 'Full snapshot-agent image reference'
    required: true
  integration_test_image:
    description: 'Optional image containing the privileged namespace-mount integration suite'
    required: false
    default: ''
  storage_size:
    description: 'Shared RWX checkpoint PVC size'
    required: false
    default: 1Gi
  storage_pvc_name:
    description: 'Shared RWX checkpoint PVC name'
    required: false
    default: snapshot-pvc
  storage_pvc_create:
    description: 'Create the checkpoint PVC; set false to use a pre-provisioned RWX claim'
    required: false
    default: 'true'
  storage_class:
    description: 'RWX-capable StorageClass for a chart-created claim (empty uses the cluster default)'
    required: false
    default: ''
  storage_base_path:
    description: 'Checkpoint PVC mount path inside snapshot-agent pods'
    required: false
    default: /checkpoints

runs:
  using: "composite"
  steps:
    - name: Setup Kubeconfig
      shell: bash
      env:
        KUBECONFIG_B64: ${{ inputs.kubeconfig_base64 }}
      run: |
        set +x
        echo "${KUBECONFIG_B64}" | base64 -d > "${{ github.workspace }}/.kubeconfig-snapshot"
        chmod 600 "${{ github.workspace }}/.kubeconfig-snapshot"
        echo "KUBECONFIG=${{ github.workspace }}/.kubeconfig-snapshot" >> "$GITHUB_ENV"

    - name: Install snapshot-agent
      shell: bash
      env:
        NAMESPACE: ${{ inputs.namespace }}
        RELEASE_NAME: ${{ inputs.release_name }}
        SNAPSHOT_AGENT_IMAGE: ${{ inputs.snapshot_agent_image }}
        STORAGE_SIZE: ${{ inputs.storage_size }}
        STORAGE_PVC_NAME: ${{ inputs.storage_pvc_name }}
        STORAGE_PVC_CREATE: ${{ inputs.storage_pvc_create }}
        STORAGE_CLASS: ${{ inputs.storage_class }}
        STORAGE_BASE_PATH: ${{ inputs.storage_base_path }}
      run: |
        echo "::group::Install snapshot-agent"
        set -euo pipefail

        SNAPSHOT_IMAGE_REPO="${SNAPSHOT_AGENT_IMAGE%:*}"
        SNAPSHOT_IMAGE_TAG="${SNAPSHOT_AGENT_IMAGE##*:}"
        if [[ -z "${SNAPSHOT_IMAGE_REPO}" || -z "${SNAPSHOT_IMAGE_TAG}" || "${SNAPSHOT_IMAGE_REPO}" == "${SNAPSHOT_AGENT_IMAGE}" ]]; then
          echo "::error::snapshot_agent_image must include an explicit tag: ${SNAPSHOT_AGENT_IMAGE}"
          exit 1
        fi

        helm upgrade --install "${RELEASE_NAME}" ./deploy/helm/charts/snapshot \
          --namespace "${NAMESPACE}" \
          --create-namespace \
          --timeout 15m \
          --set daemonset.image.repository="${SNAPSHOT_IMAGE_REPO}" \
          --set daemonset.image.tag="${SNAPSHOT_IMAGE_TAG}" \
          --set daemonset.image.pullPolicy=Always \
          --set-json=daemonset.imagePullSecrets=[] \
          --set rbac.namespaceRestricted=true \
          --set storage.pvc.name="${STORAGE_PVC_NAME}" \
          --set storage.pvc.basePath="${STORAGE_BASE_PATH}" \
          --set storage.pvc.create="${STORAGE_PVC_CREATE}" \
          --set storage.pvc.size="${STORAGE_SIZE}" \
          --set storage.pvc.storageClass="${STORAGE_CLASS}" \
          --debug

        if ! kubectl wait "pvc/${STORAGE_PVC_NAME}" \
          -n "${NAMESPACE}" \
          --for=jsonpath='{.status.phase}'=Bound \
          --timeout=300s; then
          echo "::error::checkpoint PVC ${NAMESPACE}/${STORAGE_PVC_NAME} did not bind; configure storage_class with an RWX-capable provisioner or supply an existing RWX claim"
          kubectl get pvc "${STORAGE_PVC_NAME}" -n "${NAMESPACE}" -o yaml || true
          kubectl get storageclass -o wide || true
          exit 1
        fi
        PVC_ACCESS_MODES="$(kubectl get pvc "${STORAGE_PVC_NAME}" \
          -n "${NAMESPACE}" \
          -o jsonpath='{.spec.accessModes[*]}')"
        if [[ " ${PVC_ACCESS_MODES} " != *" ReadWriteMany "* ]]; then
          echo "::error::checkpoint PVC ${NAMESPACE}/${STORAGE_PVC_NAME} is not ReadWriteMany: ${PVC_ACCESS_MODES}"
          kubectl get pvc "${STORAGE_PVC_NAME}" -n "${NAMESPACE}" -o yaml
          exit 1
        fi

        DAEMONSET_NAME="$(kubectl get daemonset \
          -n "${NAMESPACE}" \
          -l "app.kubernetes.io/name=snapshot,app.kubernetes.io/instance=${RELEASE_NAME}" \
          -o jsonpath='{.items[0].metadata.name}')"
        if [[ -z "${DAEMONSET_NAME}" ]]; then
          echo "::error::snapshot-agent DaemonSet was not created"
          exit 1
        fi

        RESTRICTED_NAMESPACE_FIELD="$(kubectl get daemonset "${DAEMONSET_NAME}" \
          -n "${NAMESPACE}" \
          -o jsonpath='{.spec.template.spec.containers[?(@.name=="agent")].env[?(@.name=="RESTRICTED_NAMESPACE")].valueFrom.fieldRef.fieldPath}')"
        if [[ "${RESTRICTED_NAMESPACE_FIELD}" != "metadata.namespace" ]]; then
          echo "::error::snapshot-agent is not configured for namespace-restricted pod watching"
          kubectl get daemonset "${DAEMONSET_NAME}" -n "${NAMESPACE}" -o yaml
          exit 1
        fi

        DESIRED_SCHEDULED="$(kubectl get daemonset "${DAEMONSET_NAME}" \
          -n "${NAMESPACE}" \
          -o jsonpath='{.status.desiredNumberScheduled}')"
        if [[ -z "${DESIRED_SCHEDULED}" || "${DESIRED_SCHEDULED}" == "0" ]]; then
          echo "::error::snapshot-agent DaemonSet matches 0 nodes in namespace ${NAMESPACE}"
          kubectl get daemonset "${DAEMONSET_NAME}" -n "${NAMESPACE}" -o wide
          kubectl get nodes -o wide --show-labels
          exit 1
        fi

        kubectl rollout status "daemonset/${DAEMONSET_NAME}" \
          -n "${NAMESPACE}" \
          --timeout=900s
        kubectl get daemonset,pods,pvc \
          -n "${NAMESPACE}" \
          -l app.kubernetes.io/name=snapshot \
          -o wide
        echo "::endgroup::"

    - name: Debug snapshot-agent failure
      if: failure()
      shell: bash
      env:
        NAMESPACE: ${{ inputs.namespace }}
      run: |
        set +e
        echo "::group::Snapshot-agent status"
        kubectl get daemonset,pods,pvc -n "${NAMESPACE}" -o wide
        echo "::endgroup::"
        echo "::group::Snapshot-agent describe"
        kubectl describe daemonset -n "${NAMESPACE}" -l app.kubernetes.io/name=snapshot
        kubectl describe pods -n "${NAMESPACE}" -l app.kubernetes.io/name=snapshot
        echo "::endgroup::"
        echo "::group::Snapshot-agent logs"
        kubectl logs -n "${NAMESPACE}" -l app.kubernetes.io/name=snapshot --all-containers --tail=100
        echo "::endgroup::"

    - name: Test snapshot namespace mounts
      if: inputs.integration_test_image != ''
      shell: bash
      env:
        NAMESPACE: ${{ inputs.namespace }}
        RELEASE_NAME: ${{ inputs.release_name }}
        INTEGRATION_TEST_IMAGE: ${{ inputs.integration_test_image }}
      run: |
        echo "::group::Test snapshot namespace mounts"
        set -euo pipefail
        JOB_NAME="${RELEASE_NAME}-nsmount-integration"
        kubectl delete job "${JOB_NAME}" -n "${NAMESPACE}" --ignore-not-found --wait=true
        kubectl apply -f - <<EOF
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: ${JOB_NAME}
          namespace: ${NAMESPACE}
        spec:
          backoffLimit: 0
          activeDeadlineSeconds: 600
          template:
            spec:
              restartPolicy: Never
              containers:
                - name: test
                  image: ${INTEGRATION_TEST_IMAGE}
                  imagePullPolicy: Always
                  securityContext:
                    privileged: true
        EOF
        if ! kubectl wait "job/${JOB_NAME}" -n "${NAMESPACE}" --for=condition=complete --timeout=600s; then
          kubectl describe job "${JOB_NAME}" -n "${NAMESPACE}" || true
          kubectl logs "job/${JOB_NAME}" -n "${NAMESPACE}" --all-containers || true
          exit 1
        fi
        kubectl logs "job/${JOB_NAME}" -n "${NAMESPACE}" --all-containers
        kubectl delete job "${JOB_NAME}" -n "${NAMESPACE}" --wait=true
        echo "::endgroup::"
