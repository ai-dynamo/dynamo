name: 'Docker Build'
description: 'Build Dynamo container images'
inputs:
  framework:
    description: 'Framework to build'
    required: true
    default: 'vllm'
  target:
    description: 'Target to build'
    required: false
    default: 'runtime'
  platform:
    description: 'Docker platform to build on, ie. linux/amd64'
    required: false
    default: 'linux/amd64'
  image_tag:
    description: 'Custom image tag (optional, defaults to framework:latest)'
    required: false
  ngc_ci_access_token:
    description: 'NGC CI Access Token'
    required: false
  ci_token:
    description: 'CI Token'
    required: false
  aws_default_region:
    description: 'AWS Default Region'
    required: false
  sccache_s3_bucket:
    description: 'SCCache S3 Bucket'
    required: false
  aws_account_id:
    description: 'AWS Account ID'
    required: false
  aws_access_key_id:
    description: 'AWS Access Key ID'
    required: false
  aws_secret_access_key:
    description: 'AWS Secret Access Key'
    required: false
  base_image_tag:
    description: 'Optional override for base image tag passed to build.sh'
    required: false
  runtime_image_tag:
    description: 'Optional override for RUNTIME_IMAGE_TAG build-arg'
    required: false
  cuda_version:
    description: 'Optional override for CUDA_VERSION build-arg'
    required: false
  torch_backend:
    description: 'Optional override for TORCH_BACKEND build-arg (e.g., cu129)'
    required: false

outputs:
  image_tag:
    description: 'Image Tag'
    value: ${{ steps.build.outputs.image_tag }}

runs:
  using: "composite"
  steps:
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 #v3.11.1
      with:
        driver: docker
        # Enable BuildKit for enhanced metadata
        buildkitd-flags: --debug
    - name: Login to ECR
      shell: bash
      env:
        ECR_HOSTNAME: ${{ inputs.aws_account_id }}.dkr.ecr.${{ inputs.aws_default_region }}.amazonaws.com
      run: |
        aws ecr get-login-password --region ${{ inputs.aws_default_region }} | docker login --username AWS --password-stdin ${ECR_HOSTNAME}
    - name: Login to NGC
      if: github.event.pull_request.head.repo.full_name == github.repository || github.event_name == 'push'
      shell: bash
      run: |
        echo "${{ inputs.ngc_ci_access_token }}" | docker login nvcr.io -u '$oauthtoken' --password-stdin
    - name: Cleanup
      if: always()
      shell: bash
      run: |
        docker system prune -af
    - name: Build image
      id: build
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.ci_token }}
        AWS_DEFAULT_REGION: ${{ inputs.aws_default_region }}
        SCCACHE_S3_BUCKET:  ${{ inputs.sccache_s3_bucket }}
        AWS_ACCESS_KEY_ID: ${{ inputs.aws_access_key_id }}
        AWS_SECRET_ACCESS_KEY: ${{ inputs.aws_secret_access_key }}
        PLATFORM: ${{ inputs.platform }}
      run: |
        echo "=========================================="
        echo "🚀 DOCKER BUILD - STARTING"
        echo "=========================================="
        
        # Determine image tag
        if [ -n "${{ inputs.image_tag }}" ]; then
          IMAGE_TAG="${{ inputs.image_tag }}"
        else
          IMAGE_TAG="${{ inputs.framework }}:latest"
        fi
        
        echo "📦 Image Tag: ${IMAGE_TAG}"
        echo "🔧 Framework: ${{ inputs.framework }}"
        echo "🎯 Target: ${{ inputs.target }}"
        echo "💻 Platform: ${{ inputs.platform }}"
        echo "🔐 Using sccache: ${{ inputs.sccache_s3_bucket != '' && 'true' || 'false' }}"

        BUILD_START_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)
        BUILD_START_EPOCH=$(date +%s)
        echo "🕐 Build started at: ${BUILD_START_TIME} (epoch: ${BUILD_START_EPOCH})"
        echo "BUILD_START_TIME=${BUILD_START_TIME}" >> $GITHUB_ENV
        echo "BUILD_START_EPOCH=${BUILD_START_EPOCH}" >> $GITHUB_ENV

        echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
        
        # Create build logs directory
        mkdir -p build-logs
        BUILD_LOG_FILE="build-logs/build-${{ inputs.framework }}-$(echo '${{ inputs.platform }}' | sed 's/linux\///').log"
        echo "BUILD_LOG_FILE=${BUILD_LOG_FILE}" >> $GITHUB_ENV
        echo "📝 Build log will be saved to: ${BUILD_LOG_FILE}"
        
        # Collect optional overrides provided by the workflow
        EXTRA_ARGS=""
        if [ -n "${{ inputs.base_image_tag }}" ]; then
          EXTRA_ARGS+=" --base-image-tag ${{ inputs.base_image_tag }}"
          echo "🔧 Base Image Tag Override: ${{ inputs.base_image_tag }}"
        fi
        if [ -n "${{ inputs.runtime_image_tag }}" ]; then
          EXTRA_ARGS+=" --build-arg RUNTIME_IMAGE_TAG=${{ inputs.runtime_image_tag }}"
          echo "🔧 Runtime Image Tag Override: ${{ inputs.runtime_image_tag }}"
        fi
        if [ -n "${{ inputs.cuda_version }}" ]; then
          EXTRA_ARGS+=" --build-arg CUDA_VERSION=${{ inputs.cuda_version }}"
          echo "🔧 CUDA Version Override: ${{ inputs.cuda_version }}"
        fi
        if [ -n "${{ inputs.torch_backend }}" ]; then
          EXTRA_ARGS+=" --build-arg TORCH_BACKEND=${{ inputs.torch_backend }}"
          echo "🔧 Torch Backend Override: ${{ inputs.torch_backend }}"
        fi
        
        echo ""
        echo "📋 Build command:"
        echo "  ./container/build.sh --tag \"${IMAGE_TAG}\" \\"
        echo "    --target ${{ inputs.target }} \\"
        echo "    --vllm-max-jobs 10 \\"
        echo "    --framework ${{ inputs.framework }} \\"
        echo "    --platform ${{ inputs.platform }} \\"
        echo "    --use-sccache \\"
        echo "    --sccache-bucket \"${SCCACHE_S3_BUCKET}\" \\"
        echo "    --sccache-region \"${AWS_DEFAULT_REGION}\" ${EXTRA_ARGS}"
        echo ""
        echo "=========================================="
        echo "🏗️  EXECUTING BUILD"
        echo "=========================================="

        # Execute build and capture output (show on console AND save to file)
        ./container/build.sh --tag "$IMAGE_TAG" \
          --target ${{ inputs.target }} \
          --vllm-max-jobs 10 \
          --framework ${{ inputs.framework }} \
          --platform ${{ inputs.platform }} \
          --use-sccache \
          --sccache-bucket "$SCCACHE_S3_BUCKET" \
          --sccache-region "$AWS_DEFAULT_REGION" $EXTRA_ARGS 2>&1 | tee "${BUILD_LOG_FILE}"
        
        BUILD_EXIT_CODE=${PIPESTATUS[0]}
        
        BUILD_END_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)
        BUILD_END_EPOCH=$(date +%s)
        BUILD_DURATION=$((BUILD_END_EPOCH - BUILD_START_EPOCH))
        
        echo ""
        echo "=========================================="
        echo "✅ BUILD COMPLETED"
        echo "=========================================="
        echo "🕐 Build ended at: ${BUILD_END_TIME} (epoch: ${BUILD_END_EPOCH})"
        echo "⏱️  Total build duration: ${BUILD_DURATION} seconds"
        echo "📝 Build log saved to: ${BUILD_LOG_FILE}"
        echo "📊 Build log size: $(wc -l < "${BUILD_LOG_FILE}") lines, $(du -h "${BUILD_LOG_FILE}" | cut -f1)"
        echo "🎯 Exit code: ${BUILD_EXIT_CODE}"
        
        echo "BUILD_END_TIME=${BUILD_END_TIME}" >> $GITHUB_ENV
        echo "BUILD_END_EPOCH=${BUILD_END_EPOCH}" >> $GITHUB_ENV
        
        # Exit with the build's exit code
        exit ${BUILD_EXIT_CODE}

    - name: Capture Build Metrics
      id: metrics
      shell: bash
      run: |
        echo "=========================================="
        echo "📊 CAPTURING BUILD METRICS"
        echo "=========================================="
        echo "Framework: ${{ inputs.framework }}"
        echo "Platform: ${{ inputs.platform }}"

        # Create metrics directory
        mkdir -p build-metrics

        # Get accurate build timing
        BUILD_START_TIME="${{ env.BUILD_START_TIME }}"
        BUILD_END_TIME="${{ env.BUILD_END_TIME }}"

        # Calculate duration
        START_EPOCH=$(date -d "$BUILD_START_TIME" +%s)
        END_EPOCH=$(date -d "$BUILD_END_TIME" +%s)
        BUILD_DURATION_SEC=$((END_EPOCH - START_EPOCH))

        echo ""
        echo "🕐 Build timing:"
        echo "  Start: ${BUILD_START_TIME}"
        echo "  End: ${BUILD_END_TIME}"
        echo "  Duration: ${BUILD_DURATION_SEC} seconds"

        # Get image size using docker inspect
        IMAGE_TAG="${{ steps.build.outputs.image_tag }}"
        if [ -n "$IMAGE_TAG" ]; then
          IMAGE_SIZE_BYTES=$(docker image inspect "$IMAGE_TAG" --format='{{.Size}}' 2>/dev/null || echo "0")
          echo ""
          echo "📦 Image size: ${IMAGE_SIZE_BYTES} bytes ($(numfmt --to=iec-i --suffix=B ${IMAGE_SIZE_BYTES} 2>/dev/null || echo 'N/A'))"
        else
          IMAGE_SIZE_BYTES=0
          echo ""
          echo "⚠️  No image tag available"
        fi

        echo ""
        echo "🔍 Platform information:"
        PLATFORM_ARCH=$(echo "${{ inputs.platform }}" | sed 's/linux\///')
        echo "  Architecture: ${PLATFORM_ARCH}"
        echo "PLATFORM_ARCH=${PLATFORM_ARCH}" >> $GITHUB_ENV
        JOB_KEY="${{ inputs.framework }}-${PLATFORM_ARCH}"
        echo "  Job Key: ${JOB_KEY}"

        # Create job-specific metrics file
        mkdir -p build-metrics
        METRICS_FILE="build-metrics/metrics-${{ inputs.framework }}-${PLATFORM_ARCH}-${{ github.run_id }}-${{ job.check_run_id }}.json"

        echo ""
        echo "💾 Writing metrics to: ${METRICS_FILE}"
        
        # Create the job metrics file
        cat > "$METRICS_FILE" << EOF
        {
          "framework": "${{ inputs.framework }}",
          "target": "${{ inputs.target }}",
          "platform": "${{ inputs.platform }}",
          "platform_arch": "${PLATFORM_ARCH}",
          "image_size_bytes": ${IMAGE_SIZE_BYTES},
          "build_start_time": "${BUILD_START_TIME}",
          "build_end_time": "${BUILD_END_TIME}",
          "build_duration_sec": ${BUILD_DURATION_SEC}
        }
        EOF

        echo ""
        echo "📄 Metrics file contents:"
        cat "$METRICS_FILE"

        echo ""
        echo "✅ Metrics captured and saved successfully"
        echo "=========================================="

    - name: Generate BuildKit Report
      id: buildkit-report
      shell: bash
      run: |
        echo "=========================================="
        echo "📊 GENERATING BUILDKIT STEP REPORT"
        echo "=========================================="
        
        BUILD_LOG_FILE="${{ env.BUILD_LOG_FILE }}"
        
        echo "Build log: ${BUILD_LOG_FILE}"
        echo ""
        
        if [ ! -f "$BUILD_LOG_FILE" ]; then
          echo "⚠️  Build log not found, skipping BuildKit report"
          exit 0
        fi
        
        # Create reports directory
        mkdir -p build-metrics/buildkit-reports
        
        PLATFORM_ARCH="${{ env.PLATFORM_ARCH }}"
        REPORT_FILE="build-metrics/buildkit-reports/buildkit-report-${{ inputs.framework }}-${PLATFORM_ARCH}.txt"
        JSON_FILE="build-metrics/buildkit-reports/buildkit-data-${{ inputs.framework }}-${PLATFORM_ARCH}.json"
        
        # Determine image name from build context
        IMAGE_NAME="${{ inputs.framework }} (${{ inputs.target }})"
        if [ "${{ inputs.target }}" == "runtime" ]; then
          IMAGE_NAME="${{ inputs.framework }} Runtime Image"
        elif [ "${{ inputs.target }}" == "dev" ]; then
          IMAGE_NAME="${{ inputs.framework }} Development Image"
        fi
        
        echo "🚀 Parsing BuildKit output..."
        echo "Image: ${IMAGE_NAME}"
        echo "Output report: ${REPORT_FILE}"
        echo "Output data: ${JSON_FILE}"
        echo ""
        
        # Make parser executable
        chmod +x .github/scripts/parse_buildkit_output.py
        
        # Run parser with image name
        set +e
        python3 .github/scripts/parse_buildkit_output.py \
          "$BUILD_LOG_FILE" \
          "$JSON_FILE" \
          "$REPORT_FILE" \
          "$IMAGE_NAME"
        PARSER_EXIT_CODE=$?
        set -e
        
        echo ""
        echo "📊 Parser exit code: ${PARSER_EXIT_CODE}"
        
        if [ ${PARSER_EXIT_CODE} -eq 0 ] && [ -f "$REPORT_FILE" ]; then
          echo ""
          echo "✅ BuildKit report generated successfully"
          echo "📄 Report file: $REPORT_FILE"
          echo "📄 JSON file: $JSON_FILE"
          echo ""
          
          # Display the report
          echo "=========================================="
          echo "📊 BUILDKIT REPORT PREVIEW"
          echo "=========================================="
          cat "$REPORT_FILE"
          echo ""
        else
          echo "⚠️  BuildKit report generation had issues but continuing..."
        fi
        
        echo "✅ BuildKit report step completed"
        echo "=========================================="

    # Upload job-specific build metrics as artifact
    - name: Upload Build Metrics
      uses: actions/upload-artifact@v4
      with:
        name: build-metrics-${{ inputs.framework }}-${{ env.PLATFORM_ARCH }}-${{ github.run_id }}-${{ job.check_run_id }}
        path: build-metrics/metrics-${{ inputs.framework }}-${{ env.PLATFORM_ARCH }}-${{ github.run_id }}-${{ job.check_run_id }}.json
        retention-days: 7

    # Upload build logs as artifact for debugging and analysis
    - name: Upload Build Logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: build-logs-${{ inputs.framework }}-${{ env.PLATFORM_ARCH }}-${{ github.run_id }}-${{ job.check_run_id }}
        path: build-logs/*.log
        retention-days: 7

    # Upload BuildKit reports as artifact
    - name: Upload BuildKit Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: buildkit-reports-${{ inputs.framework }}-${{ env.PLATFORM_ARCH }}-${{ github.run_id }}-${{ job.check_run_id }}
        path: build-metrics/buildkit-reports/*
        retention-days: 7
