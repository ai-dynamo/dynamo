# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Intel XPU deployment example for multimodal model.
#
# This example demonstrates how to deploy the multimodal pipeline on Intel XPU
# using Dynamic Resource Allocation (DRA) for GPU access.
#
# Prerequisites:
# - Kubernetes cluster with Intel XPU devices
# - Intel GPU Resource Driver installed (DRA)
# - ResourceClaimTemplate for gpu.intel.com device class
#
# Key differences from standard GPU deployment:
# 1. Uses DRA (resourceClaims) instead of resources.limits.gpu
# 2. Sets VLLM_TARGET_DEVICE=xpu environment variable

apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: gpu-template-xe
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: gpu.intel.com
            count: 1
---
apiVersion: nvidia.com/v1alpha1
kind: DynamoGraphDeployment
metadata:
  name: agg-qwen-xpu
spec:
  backendFramework: vllm
  services:
    Frontend:
      componentType: frontend
      replicas: 1
      extraPodSpec:
        mainContainer:
          image: my-registry/vllm-runtime:my-tag
    EncodeWorker:
      envFromSecret: hf-token-secret
      componentType: worker
      replicas: 1
      extraPodSpec:
        resourceClaims:
          - name: gpu
            resourceClaimTemplateName: gpu-template-xe
        mainContainer:
          image: my-registry/vllm-runtime:my-tag
          workingDir: /workspace/examples/multimodal
          env:
            - name: VLLM_TARGET_DEVICE
              value: xpu
          resources:
            claims:
              - name: gpu
          command:
            - /bin/sh
            - -c
          args:
            - python3 components/encode_worker.py --model Qwen/Qwen2.5-VL-7B-Instruct
    VLMWorker:
      envFromSecret: hf-token-secret
      componentType: worker
      replicas: 1
      extraPodSpec:
        resourceClaims:
          - name: gpu
            resourceClaimTemplateName: gpu-template-xe
        mainContainer:
          image: my-registry/vllm-runtime:my-tag
          workingDir: /workspace/examples/multimodal
          env:
            - name: VLLM_TARGET_DEVICE
              value: xpu
          resources:
            claims:
              - name: gpu
          command:
            - /bin/sh
            - -c
          args:
            - python3 components/worker.py --model Qwen/Qwen2.5-VL-7B-Instruct --worker-type prefill
    Processor:
      envFromSecret: hf-token-secret
      componentType: worker
      replicas: 1
      extraPodSpec:
        resourceClaims:
          - name: gpu
            resourceClaimTemplateName: gpu-template-xe
        mainContainer:
          image: my-registry/vllm-runtime:my-tag
          workingDir: /workspace/examples/multimodal
          env:
            - name: VLLM_TARGET_DEVICE
              value: xpu
          resources:
            claims:
              - name: gpu
          command:
            - /bin/sh
            - -c
          args:
            - 'python3 components/processor.py --model Qwen/Qwen2.5-VL-7B-Instruct --prompt-template "<|im_start|>system\nYou are a helpful assistant.<|im_end|>\n<|im_start|>user\n<|vision_start|><|image_pad|><|vision_end|><prompt><|im_end|>\n<|im_start|>assistant\n"'
