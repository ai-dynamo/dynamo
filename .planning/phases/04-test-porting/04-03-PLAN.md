---
phase: 04-test-porting
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - lib/kvbm-connector/src/testing/mod.rs
  - lib/kvbm-connector/src/testing/e2e/mod.rs
  - lib/kvbm-connector/src/testing/e2e/find_blocks.rs
  - lib/kvbm-connector/src/testing/e2e/s3_object.rs
  - lib/kvbm-connector/src/testing/scheduler/mod.rs
  - lib/kvbm-connector/src/testing/scheduler/connector_tests.rs
  - lib/kvbm-connector/src/testing/scheduler/mock/mod.rs
  - lib/kvbm-connector/src/testing/scheduler/mock/engine.rs
  - lib/kvbm-connector/src/testing/scheduler/mock/model.rs
  - lib/kvbm-connector/src/testing/scheduler/mock/tests.rs
  - lib/kvbm-connector/src/testing/scheduler/mock/abort_tests.rs
  - lib/kvbm-connector/src/testing/scheduler/mock/connector_e2e_tests.rs
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "All 11 files under `src/testing/e2e/` and `src/testing/scheduler/` are created"
    - "`cargo check -p kvbm-connector --features testing` passes after all files are present"
    - "Every test function that imports from `v2::integrations::scheduler::Scheduler` is disabled with `#[cfg(TODO)]` and a comment"
    - "`s3_object.rs` is gated with `#[cfg(TODO)]` (s3 feature not declared in kvbm-connector)"
    - "`testing/mod.rs` has `pub mod e2e` and `pub mod scheduler` uncommented"
  artifacts:
    - path: lib/kvbm-connector/src/testing/e2e/mod.rs
      provides: e2e cluster test module
    - path: lib/kvbm-connector/src/testing/e2e/find_blocks.rs
      provides: block-finding e2e tests
    - path: lib/kvbm-connector/src/testing/scheduler/mod.rs
      provides: scheduler test utilities (scheduler-dependent functions disabled)
    - path: lib/kvbm-connector/src/testing/scheduler/mock/engine.rs
      provides: mock engine for CPU-only scheduler testing
  key_links:
    - from: lib/kvbm-connector/src/testing/e2e/mod.rs
      to: lib/kvbm-connector/src/testing/connector.rs
      via: "use crate::testing::connector::{ConnectorTestConfig, TestConnectorCluster}"
      pattern: "crate::testing::connector"
    - from: lib/kvbm-connector/src/testing/scheduler/mod.rs
      to: lib/kvbm-connector/src/testing/connector.rs
      via: "use crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance}"
      pattern: "crate::testing::connector"
---

<objective>
Port the `e2e/` and `scheduler/` subdirectories from `ryan/kvbm-next:lib/kvbm/src/v2/testing/` into `lib/kvbm-connector/src/testing/`, applying import migration. Disable all tests that depend on `v2::integrations::scheduler::Scheduler` (which has no workspace equivalent). Uncomment `pub mod e2e` and `pub mod scheduler` in `testing/mod.rs`.

Purpose: Completes TEST-02 by bringing in all connector-specific test files. The scheduler tests are structurally present but gated with `#[cfg(TODO)]` so the crate compiles cleanly.

Output: 11 new files under `src/testing/e2e/` and `src/testing/scheduler/`, updated `testing/mod.rs`.
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-test-porting/04-CONTEXT.md
@.planning/phases/04-test-porting/04-RESEARCH.md
@.planning/phases/04-test-porting/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Import mapping for e2e/ and scheduler/ files -->
<!--
Same table as Plan 02, plus:
crate::v2::testing::connector::{ConnectorTestConfig, TestConnectorCluster, TestConnectorInstance}
  → crate::testing::connector::{ConnectorTestConfig, TestConnectorCluster, TestConnectorInstance}

crate::v2::testing::{managers, token_blocks}
  → kvbm_engine::testing::{managers, token_blocks}

crate::v2::integrations::common::{Request, SchedulerOutput}
  → crate::common::{Request, SchedulerOutput}

crate::v2::integrations::connector::leader::ConnectorLeader
  → crate::connector::leader::ConnectorLeader

crate::v2::integrations::scheduler::{Scheduler, KVCacheManager, SchedulerConfig, ...}
  → NO EQUIVALENT — gate entire function/test/module with #[cfg(TODO)]

crate::v2::G1 → crate::G1
crate::G1 → crate::G1 (unchanged if already correct)
-->

<!-- Disabled test annotation pattern -->
```rust
// Source: [original test/function name]
// TODO: Disabled — depends on v2::integrations::scheduler::Scheduler which has no
// workspace equivalent in this phase. Re-enable when integrations/scheduler is ported.
#[cfg(TODO)]
#[tokio::test]
async fn test_that_uses_scheduler() { ... }
```

<!-- Files known to need full #[cfg(TODO)] wrapping -->
<!-- scheduler/connector_tests.rs — entire file is #[cfg(test)] using Scheduler -->
<!-- scheduler/mock/tests.rs — all tests use Scheduler -->
<!-- scheduler/mock/abort_tests.rs — all tests use Scheduler -->
<!-- scheduler/mock/connector_e2e_tests.rs — all tests use Scheduler -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port testing/e2e/ (3 files)</name>
  <files>
    lib/kvbm-connector/src/testing/e2e/mod.rs,
    lib/kvbm-connector/src/testing/e2e/find_blocks.rs,
    lib/kvbm-connector/src/testing/e2e/s3_object.rs
  </files>
  <action>
Read each source file:
```
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/e2e/mod.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/e2e/find_blocks.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/e2e/s3_object.rs
```

**e2e/mod.rs:** Port verbatim. Apply import migration:
- `crate::v2::physical::transfer::FillPattern` → `kvbm_physical::transfer::FillPattern`
- `crate::v2::testing::connector::*` → `crate::testing::connector::*`
- Any `super::nova` → `kvbm_engine::testing::messenger`
- `#[cfg(test)] mod find_blocks;` → keep as `#[cfg(test)] mod find_blocks;`
- `#[cfg(all(test, feature = "s3"))] mod s3_object;` → change to `#[cfg(TODO)] mod s3_object;` with comment:
  ```
  // TODO: s3 feature not declared in kvbm-connector. Disabled until s3 is added.
  #[cfg(TODO)]
  mod s3_object;
  ```

**e2e/find_blocks.rs:** Port verbatim. Apply import migration. This file is `#[cfg(test)]` inline tests only.

**e2e/s3_object.rs:** Port verbatim (do not delete), but wrap the entire module content in `#[cfg(TODO)]` or add a note at the top:
```rust
// TODO: Disabled — requires `s3` feature which is not declared in kvbm-connector/Cargo.toml.
// Re-enable by declaring the feature and uncommenting.
#[cfg(TODO)]
mod s3_disabled {
    // [original file content here]
}
```

After creating all three files, run `cargo check -p kvbm-connector --features testing` and fix any errors.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger && cargo check -p kvbm-connector --features testing 2>&1 | tail -10</automated>
  </verify>
  <done>
- `testing/e2e/mod.rs`, `testing/e2e/find_blocks.rs`, `testing/e2e/s3_object.rs` all exist
- `s3_object.rs` is entirely gated so it doesn't compile without the s3 feature
- `cargo check -p kvbm-connector --features testing` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Port testing/scheduler/ (8 files) and activate all submodules</name>
  <files>
    lib/kvbm-connector/src/testing/scheduler/mod.rs,
    lib/kvbm-connector/src/testing/scheduler/connector_tests.rs,
    lib/kvbm-connector/src/testing/scheduler/mock/mod.rs,
    lib/kvbm-connector/src/testing/scheduler/mock/engine.rs,
    lib/kvbm-connector/src/testing/scheduler/mock/model.rs,
    lib/kvbm-connector/src/testing/scheduler/mock/tests.rs,
    lib/kvbm-connector/src/testing/scheduler/mock/abort_tests.rs,
    lib/kvbm-connector/src/testing/scheduler/mock/connector_e2e_tests.rs,
    lib/kvbm-connector/src/testing/mod.rs
  </files>
  <action>
Read each source file:
```
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mod.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/connector_tests.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mock/mod.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mock/engine.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mock/model.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mock/tests.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mock/abort_tests.rs
git show ryan/kvbm-next:lib/kvbm/src/v2/testing/scheduler/mock/connector_e2e_tests.rs
```

**scheduler/mod.rs:** Port verbatim. Apply import migration:
- `crate::v2::integrations::scheduler::{KVCacheManager, RequestStatus, Scheduler, SchedulerConfig}` → these have NO workspace equivalent. Gate the entire function that uses them with `#[cfg(TODO)]` with a comment.
- `crate::v2::SequenceHash` → `crate::SequenceHash`
- `crate::v2::integrations::common::Request` → `crate::common::Request`
- `crate::v2::integrations::connector::leader::ConnectorLeader` → `crate::connector::leader::ConnectorLeader`
- `super::connector::{ConnectorTestConfig, TestConnectorInstance}` → `crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance}`
- `super::managers` → `kvbm_engine::testing::managers`
- `super::token_blocks` → `kvbm_engine::testing::token_blocks`
- `crate::G1` → `crate::G1` (keep)
- `kvbm_logical::blocks::BlockRegistry` — check actual path in workspace

**scheduler/connector_tests.rs:** The entire file is `#[cfg(test)]` and uses `Scheduler`. Gate the whole file's test content with `#[cfg(TODO)]`. Pattern:
```rust
// TODO: Entire file disabled — uses v2::integrations::scheduler::Scheduler which has
// no workspace equivalent. Re-enable when integrations/scheduler is ported.
#[cfg(TODO)]
mod connector_tests_disabled {
    // [original file content]
}
```

**scheduler/mock/mod.rs:** Port verbatim. Apply import migration. Declare submodules. Scheduler-using code gated with `#[cfg(TODO)]`.

**scheduler/mock/engine.rs:** Port verbatim. Apply import migration. Gate any function/struct that imports `Scheduler` with `#[cfg(TODO)]`.

**scheduler/mock/model.rs:** Port verbatim. Apply import migration.

**scheduler/mock/tests.rs, abort_tests.rs, connector_e2e_tests.rs:** Each file is `#[cfg(test)]` and tests use `Scheduler`. Gate each file's content entirely with `#[cfg(TODO)]` using the same pattern as connector_tests.rs.

**After all scheduler files are created, update testing/mod.rs** to uncomment `pub mod e2e` and `pub mod scheduler`:
```rust
pub mod connector;
pub mod e2e;
pub mod scheduler;

pub use connector::{
    ConnectorTestConfig,
    TestConnectorInstance,
    TestConnectorCluster,
    TestConnectorWorker,
};
```

Run `cargo check -p kvbm-connector --features testing` and fix errors iteratively. If an import error appears from `v2::integrations::scheduler`, gate it. Do NOT attempt to provide a stub implementation.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger && cargo check -p kvbm-connector --features testing 2>&1 | tail -15</automated>
  </verify>
  <done>
- All 8 scheduler files exist under `src/testing/scheduler/`
- `testing/mod.rs` has `pub mod e2e` and `pub mod scheduler` active
- `cargo check -p kvbm-connector --features testing` exits with zero errors
- All test functions using `Scheduler` are wrapped in `#[cfg(TODO)]` with explanatory comment
- TEST-02 fully satisfied: all connector-specific test files ported
  </done>
</task>

</tasks>

<verification>
After both tasks:
```
cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger
cargo check -p kvbm-connector --features testing
```
Must pass with zero errors.

Count the ported files:
```
ls lib/kvbm-connector/src/testing/e2e/ lib/kvbm-connector/src/testing/scheduler/ lib/kvbm-connector/src/testing/scheduler/mock/
```
Must show: mod.rs + find_blocks.rs + s3_object.rs | mod.rs + connector_tests.rs | mod.rs + engine.rs + model.rs + tests.rs + abort_tests.rs + connector_e2e_tests.rs

Confirm disabled tests annotation is correct:
```
grep -r "cfg(TODO)" lib/kvbm-connector/src/testing/scheduler/
```
Must appear in scheduler tests.
</verification>

<success_criteria>
- All 11 new files exist under `src/testing/e2e/` and `src/testing/scheduler/`
- `cargo check -p kvbm-connector --features testing` passes with zero errors
- `testing/mod.rs` exposes `e2e` and `scheduler` as submodules
- No live code imports `v2::integrations::scheduler` — all such code is `#[cfg(TODO)]`
- TEST-02 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-porting/04-03-SUMMARY.md`
</output>
