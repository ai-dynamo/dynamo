---
phase: 04-test-porting
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - lib/kvbm-connector/src/connector/worker/tests.rs
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "`cargo test -p kvbm-connector --features testing --no-run` exits with zero errors (TEST-03)"
    - "`cargo test -p kvbm-connector --features testing` passes with all tests green (TEST-04)"
    - "`worker/tests.rs` imports `crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance}` without the TODO comment"
    - "No enabled test references an unresolved import"
  artifacts:
    - path: lib/kvbm-connector/src/connector/worker/tests.rs
      provides: worker unit tests with restored imports
      contains: "use crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance}"
  key_links:
    - from: lib/kvbm-connector/src/connector/worker/tests.rs
      to: lib/kvbm-connector/src/testing/connector.rs
      via: "use crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance}"
      pattern: "crate::testing::connector"
    - from: lib/kvbm-connector/src/connector/worker/mod.rs
      to: lib/kvbm-connector/src/connector/worker/tests.rs
      via: "#[cfg(all(test, feature = \"testing\"))] mod tests;"
      pattern: "cfg.*all.*test.*testing.*mod tests"
---

<objective>
Restore the commented-out import in `connector/worker/tests.rs` (replacing the Phase 2 TODO placeholder), run the full test suite, and fix any remaining compilation or test-failure issues until `cargo test -p kvbm-connector --features testing` is fully green.

Purpose: This is the final validation gate. TEST-03 (tests compile) and TEST-04 (tests pass) are proven here.

Output: Updated `worker/tests.rs` with restored imports, fully green test run.
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-test-porting/04-CONTEXT.md
@.planning/phases/04-test-porting/04-RESEARCH.md
@.planning/phases/04-test-porting/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Current state of worker/tests.rs import placeholder (from Phase 2) -->
// TODO(Phase 4): Restore when testing infra is ported
// use crate::v2::testing::connector::{ConnectorTestConfig, TestConnectorInstance};

<!-- Correct import after restoration -->
use crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance};

<!-- worker/mod.rs gate — DO NOT CHANGE THIS LINE -->
#[cfg(all(test, feature = "testing"))]
mod tests;

<!-- Test command -->
cargo test -p kvbm-connector --features testing -- --test-output immediate 2>&1 | tail -30
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restore worker/tests.rs imports and compile-check</name>
  <files>lib/kvbm-connector/src/connector/worker/tests.rs</files>
  <action>
Read the current state of `lib/kvbm-connector/src/connector/worker/tests.rs`.

Find the TODO placeholder block near the top of the file:
```rust
// TODO(Phase 4): Restore when testing infra is ported
// use crate::v2::testing::connector::{ConnectorTestConfig, TestConnectorInstance};
```

Replace with the correct import:
```rust
use crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance};
```

Remove the TODO comment — the infra is now ported.

Also scan the file for any remaining `crate::v2::` references — if any exist, apply the same import migration table from Phase 2/4 research. There should be none, as the file was pre-staged, but verify.

After the change, run:
```
cargo test -p kvbm-connector --features testing --no-run
```
This is the TEST-03 check: compile-only, no test execution. Fix any compilation errors before proceeding to Task 2.

NOTE: Do NOT change `#[cfg(all(test, feature = "testing"))] mod tests;` in `worker/mod.rs` — the gate is correct as-is.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger && cargo test -p kvbm-connector --features testing --no-run 2>&1 | tail -15</automated>
  </verify>
  <done>
- `worker/tests.rs` has `use crate::testing::connector::{ConnectorTestConfig, TestConnectorInstance};` (no TODO comment)
- `cargo test -p kvbm-connector --features testing --no-run` exits with zero errors
- TEST-03 satisfied
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and fix failures</name>
  <files>lib/kvbm-connector/src/connector/worker/tests.rs</files>
  <action>
Run the full test suite:
```
cargo test -p kvbm-connector --features testing -- --test-output immediate 2>&1
```

**Expected outcome:** All tests in `connector/worker/tests.rs` pass. The tests exercise flag lifecycle (`intra_pass_onboard_active`, `intra_pass_offload_active`, `needs_offload_action`) and early-exit paths — all without GPU hardware, because `TestConnectorInstance` uses `NixlConfig::default()` with POSIX/UCX backends.

**If a test fails due to GPU/NIXL hardware not being present:**
- Check if `ConnectorTestConfig` supports `worker_without_nixl()` or a CPU-only path
- If yes: update `create_test_instance()` in tests.rs to use the CPU-only config
- If no CPU path exists and the test requires GPU: wrap the test with `#[cfg(TODO)]` and a comment: `// TODO: Requires NIXL/GPU hardware. Disabled in CPU-only CI.`

**If a test panics during `instance.initialize().await`:**
- This may indicate that `TestConnectorInstance::initialize()` calls into RDMA/NIXL init which requires hardware
- Investigate whether `worker_without_nixl()` or `NixlConfig::empty()` bypasses hardware deps
- Apply minimally — only wrap specific failing tests, not the entire file

**STOP condition:** If more than 3 tests are failing for hardware reasons, report back to user rather than disabling wholesale. The goal is to have a working test suite, not a fully-disabled one.

After reaching a stable green (or documented-disabled) state, run once more:
```
cargo test -p kvbm-connector --features testing
```
And capture the final output.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger && cargo test -p kvbm-connector --features testing 2>&1 | tail -20</automated>
  </verify>
  <done>
- `cargo test -p kvbm-connector --features testing` exits with zero test failures (FAILED count = 0)
- All enabled tests pass; any hardware-only tests are marked `#[cfg(TODO)]` or `#[ignore]` with explanatory comment
- TEST-04 satisfied
  </done>
</task>

</tasks>

<verification>
Final phase gate:
```
cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger
cargo test -p kvbm-connector --features testing
```
Output must contain `test result: ok` with 0 failures.

Workspace regression check:
```
cargo check --workspace
```
Must still pass — no regressions from adding dev-deps or testing module.
</verification>

<success_criteria>
- `cargo test -p kvbm-connector --features testing --no-run` passes (TEST-03)
- `cargo test -p kvbm-connector --features testing` passes with 0 failures (TEST-04)
- `cargo check --workspace` passes — no regressions
- All four TEST-01 through TEST-04 requirements satisfied
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-porting/04-04-SUMMARY.md`
</output>
