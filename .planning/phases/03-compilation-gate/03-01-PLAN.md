---
phase: 03-compilation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kvbm-connector/Cargo.toml
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "`cargo check -p kvbm-connector` exits with zero errors"
    - "`cargo check -p kvbm-connector` exits with zero warnings"
    - "`cargo check --workspace` exits with zero errors (no regressions in other crates)"
  artifacts:
    - path: "lib/kvbm-connector/Cargo.toml"
      provides: "nccl feature declaration"
      contains: "nccl = [\"kvbm-engine/nccl\"]"
  key_links:
    - from: "lib/kvbm-connector/src/connector/worker/mod.rs"
      to: "lib/kvbm-connector/Cargo.toml"
      via: "#[cfg(feature = \"nccl\")] at lines 530 and 552"
      pattern: "nccl.*kvbm-engine"
---

<objective>
Declare the `nccl` feature in `kvbm-connector/Cargo.toml`, eliminating two `unexpected_cfg` compiler warnings, then confirm both the crate and full workspace compile cleanly.

Purpose: Phase 3's only open issue is 2 Rust compiler warnings — `unexpected cfg condition value: nccl` — caused by `#[cfg(feature = "nccl")]` usage in `worker/mod.rs` with no corresponding `[features]` declaration. Fixing this satisfies COMP-01, COMP-02, and COMP-03.

Output: `lib/kvbm-connector/Cargo.toml` with `nccl` feature declared; `cargo check -p kvbm-connector` and `cargo check --workspace` both green.
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current [features] section in lib/kvbm-connector/Cargo.toml (read directly) -->
```toml
[features]
testing = [
    "kvbm-engine/testing",
    "kvbm-logical/testing",
    "kvbm-physical/testing",
]
```

<!-- kvbm-engine feature that nccl must wire through (existing, verified) -->
<!-- lib/kvbm-engine/Cargo.toml line 64: nccl = ["dep:cudarc"] -->

<!-- Usage sites in lib/kvbm-connector/src/connector/worker/mod.rs -->
<!-- Line 530: #[cfg(feature = "nccl")] — gates execute_local_layerwise_onboard call -->
<!-- Line 552: #[cfg(not(feature = "nccl"))] — emits tracing::warn when NCCL unavailable -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare nccl feature in kvbm-connector Cargo.toml</name>
  <files>lib/kvbm-connector/Cargo.toml</files>
  <action>
Add `nccl = ["kvbm-engine/nccl"]` as a top-level entry in the `[features]` table of `lib/kvbm-connector/Cargo.toml`, immediately after the closing `]` of the `testing` feature block.

The entry is a passthrough: kvbm-connector does not implement NCCL itself — it gates a call to `worker.execute_local_layerwise_onboard()` behind `#[cfg(feature = "nccl")]`. Enabling this feature on the connector propagates to the engine layer that has the real implementation.

Exact change — append after the `testing = [...]` block:

```toml
# Gates NCCL-accelerated intra-pass layer-wise onboard (G2→G1 via cudarc).
nccl = ["kvbm-engine/nccl"]
```

DO NOT add `collectives` or `testing-nccl` (per user decision: YAGNI, minimize diff).
DO NOT modify `[workspace.dependencies]` (no downstream crates currently depend on kvbm-connector).
DO NOT suppress the warning with `#[allow(unexpected_cfgs)]` — declare the feature properly.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger && cargo check -p kvbm-connector 2>&1</automated>
  </verify>
  <done>`cargo check -p kvbm-connector` finishes with zero errors and zero warnings. The two `unexpected cfg condition value: nccl` warnings are gone. Output ends with `Finished` and no warning count line for kvbm-connector.</done>
</task>

<task type="auto">
  <name>Task 2: Workspace regression check</name>
  <files></files>
  <action>
Run `cargo check --workspace` to verify the Cargo.toml change does not break any other crate in the workspace. No code changes are expected — this is a read-only verification step.

If the workspace check surfaces errors in other crates that are NOT related to the `nccl` feature addition, document them as pre-existing issues in the SUMMARY (they are not caused by this change and fall outside phase scope).
  </action>
  <verify>
    <automated>cd /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger && cargo check --workspace 2>&1</automated>
  </verify>
  <done>`cargo check --workspace` exits with zero errors. Any warnings present are not attributed to kvbm-connector and are pre-existing. COMP-03 satisfied.</done>
</task>

</tasks>

<verification>
After both tasks:
- `cargo check -p kvbm-connector` → Finished, 0 errors, 0 warnings
- `cargo check --workspace` → Finished, 0 errors (regressions ruled out)
- `lib/kvbm-connector/Cargo.toml` `[features]` section contains both `testing` and `nccl` entries
- `nccl` entry value is exactly `["kvbm-engine/nccl"]` — not `["dep:cudarc"]` (that is the engine's internal dep, not what the connector should declare)
</verification>

<success_criteria>
Phase 3 complete when:
1. `cargo check -p kvbm-connector` — zero errors, zero warnings (COMP-01, COMP-02)
2. `cargo check --workspace` — zero errors (COMP-03)
3. No new warnings introduced in any other workspace crate
</success_criteria>

<output>
After completion, create `.planning/phases/03-compilation-gate/03-01-SUMMARY.md` following the summary template at `@/home/ryan/.claude/get-shit-done/templates/summary.md`.
</output>
