---
phase: 02-import-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/kvbm-connector/src/config.rs
  - lib/kvbm-connector/src/common/block_assignments.rs
  - lib/kvbm-connector/src/common/output.rs
  - lib/kvbm-connector/src/connector/leader/mod.rs
  - lib/kvbm-connector/src/connector/leader/init.rs
  - lib/kvbm-connector/src/connector/leader/slot.rs
  - lib/kvbm-connector/src/connector/leader/scheduler.rs
  - lib/kvbm-connector/src/connector/worker/mod.rs
  - lib/kvbm-connector/src/connector/worker/nova/client.rs
  - lib/kvbm-connector/src/connector/worker/init/pending.rs
  - lib/kvbm-connector/src/connector/worker/tests.rs
  - lib/kvbm-connector/src/vllm/config.rs
autonomous: true
requirements:
  - IMP-01
  - IMP-05

must_haves:
  truths:
    - "No crate::v2::* import paths remain in lib/kvbm-connector/src/"
    - "No crate::integrations::* import paths remain in lib/kvbm-connector/src/"
    - "connector/worker/tests.rs is gated behind #[cfg(all(test, feature = \"testing\"))] with broken test imports commented out as Phase 4 TODOs"
    - "cargo check -p kvbm-connector passes after pass 4 and again after pass 5"
  artifacts:
    - path: "lib/kvbm-connector/src/config.rs"
      provides: "crate::v2 imports replaced with kvbm_common"
    - path: "lib/kvbm-connector/src/common/block_assignments.rs"
      provides: "crate::v2 imports replaced with kvbm_common/kvbm_logical"
    - path: "lib/kvbm-connector/src/connector/worker/tests.rs"
      provides: "test file gated behind cfg(all(test, feature = testing))"
    - path: "lib/kvbm-connector/src/vllm/config.rs"
      provides: "crate::v2::integrations::config self-ref resolved to crate::config"
  key_links:
    - from: "config.rs"
      to: "kvbm_common::{CacheLayout, ModelExecutorBackend}"
      via: "use kvbm_common::{CacheLayout, ModelExecutorBackend}"
      pattern: "kvbm_common::"
    - from: "connector/leader/scheduler.rs"
      to: "crate::common::{CachedRequestData, NewRequestData, SchedulerOutput}"
      via: "use crate::common::{...}"
      pattern: "crate::common::"
    - from: "vllm/config.rs"
      to: "crate::config::{AttentionConfig, IntegrationsConfig, ParallelConfig}"
      via: "use crate::config::{...}"
      pattern: "crate::config::"
---

<objective>
Fix the remaining two broken import namespaces: crate::v2::* (Pass 4) and crate::integrations::* self-refs (Pass 5). Also gate the test file with the correct cfg attributes, deferring test import resolution to Phase 4.

Purpose: After Plan 01 clears the three simple namespaces, the v2:: and integrations:: namespaces are the final structural import breakage. These are more complex because v2:: references scatter across multiple destination crates (kvbm_common, kvbm_engine, kvbm_logical, kvbm_physical, local crate::*). Pass 5 is trivial once Pass 4 is done — integrations:: self-refs just strip the integrations:: prefix.
Output: Two commits (Pass 4, Pass 5). cargo check -p kvbm-connector passes after each. No v2:: or integrations:: import paths remain.
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-import-migration/02-CONTEXT.md
@.planning/phases/02-import-migration/02-RESEARCH.md
@.planning/phases/02-import-migration/02-01-SUMMARY.md

<interfaces>
<!-- Pre-verified type locations. Do not re-research these. -->

kvbm_common exports (use directly):
```rust
kvbm_common::BlockId
kvbm_common::SequenceHash
kvbm_common::CacheLayout      // verify first — see open question 3 in RESEARCH.md
kvbm_common::ModelExecutorBackend  // verify first
kvbm_common::LogicalLayoutHandle
```

kvbm_logical exports:
```rust
kvbm_logical::blocks::{BlockMetadata, ImmutableBlock, MutableBlock}
kvbm_logical::KvbmSequenceHashProvider
```

kvbm_engine exports (beyond what Pass 3 already fixed):
```rust
kvbm_engine::leader::InstanceLeader
kvbm_engine::offload::OffloadEngine
kvbm_engine::worker::SerializedLayout
kvbm_engine::object::{ObjectLockManager, create_lock_manager, create_object_client, ObjectBlockOps}
kvbm_engine::offload::{ObjectPipelineBuilder, ObjectPresenceFilter, OffloadEngine,
                        PendingTracker, PipelineBuilder, S3PresenceChecker, create_policy_from_config}
kvbm_engine::worker::{DirectWorker, WorkerTransfers}
```

Local crate paths (crate:: = lib/kvbm-connector/src/lib.rs = old integrations module):
```
crate::connector::leader::scheduler::KvConnectorMetadata
crate::connector::leader::slot::RequestSlot
crate::connector::worker::ConnectorWorkerClient
crate::vllm::layout::determine_kv_layout
crate::common::{CachedRequestData, NewRequestData, SchedulerOutput}
crate::config::{AttentionConfig, IntegrationsConfig, ParallelConfig}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass 4 — Replace crate::v2::* imports (IMP-01) + gate tests.rs</name>
  <files>
    lib/kvbm-connector/src/config.rs
    lib/kvbm-connector/src/common/block_assignments.rs
    lib/kvbm-connector/src/common/output.rs
    lib/kvbm-connector/src/connector/leader/mod.rs
    lib/kvbm-connector/src/connector/leader/init.rs
    lib/kvbm-connector/src/connector/leader/slot.rs
    lib/kvbm-connector/src/connector/leader/scheduler.rs
    lib/kvbm-connector/src/connector/worker/mod.rs
    lib/kvbm-connector/src/connector/worker/nova/client.rs
    lib/kvbm-connector/src/connector/worker/init/pending.rs
    lib/kvbm-connector/src/connector/worker/tests.rs
  </files>
  <action>
Fix every `crate::v2::*` import in kvbm-connector. This is Pass 4. Also gate the test file in this pass (it contains crate::v2::testing:: imports that belong to Phase 4).

**BEFORE STARTING — verify two open questions from RESEARCH.md:**
1. Run: `grep -rn "CacheLayout\|ModelExecutorBackend" /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger/lib/kvbm-common/src/` to confirm the path for config.rs line 14. If not in kvbm_common, check kvbm_engine.
2. Run: `grep -rn "TokioRuntime" /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger/lib/kvbm-physical/src/` to confirm whether it exists in kvbm_physical (relevant for pending.rs — may already be fixed in Pass 2 or may need runtime.tokio() substitution).

Apply the full Pass 4 mapping from RESEARCH.md:

**config.rs (line 14):**
- `crate::v2::{CacheLayout, ModelExecutorBackend}` → `kvbm_common::{CacheLayout, ModelExecutorBackend}` (use verified path from pre-check above)

**common/block_assignments.rs (lines 24, 363):**
- `crate::v2::{BlockId, SequenceHash}` → `kvbm_common::{BlockId, SequenceHash}`
- `crate::v2::logical::blocks::{BlockMetadata, ImmutableBlock, MutableBlock}` → `kvbm_logical::blocks::{BlockMetadata, ImmutableBlock, MutableBlock}`

**common/output.rs (lines 6-7):**
- `crate::v2::BlockId` → `kvbm_common::BlockId`
- `crate::v2::integrations::connector::leader::scheduler::KvConnectorMetadata` → `crate::connector::leader::scheduler::KvConnectorMetadata` (IMP-05 concern but caught here)

**connector/leader/mod.rs (lines 13-16, if not already fixed in Pass 3):**
- `crate::v2::distributed::leader::InstanceLeader` → `kvbm_engine::leader::InstanceLeader`
- `crate::v2::distributed::offload::OffloadEngine` → `kvbm_engine::offload::OffloadEngine`
- `crate::v2::distributed::worker::SerializedLayout` → `kvbm_engine::worker::SerializedLayout`
- `crate::v2::logical::blocks::ImmutableBlock` → `kvbm_logical::blocks::ImmutableBlock`

**connector/leader/slot.rs (line 13):**
- `crate::v2::{BlockId, KvbmSequenceHashProvider, SequenceHash}` → `kvbm_common::{BlockId, SequenceHash}` + `kvbm_logical::KvbmSequenceHashProvider`

**connector/leader/init.rs (lines 14-20, 504):**
- `crate::v2::distributed::object::{ObjectLockManager, create_lock_manager, create_object_client}` → `kvbm_engine::object::{ObjectLockManager, create_lock_manager, create_object_client}`
- `crate::v2::distributed::offload::{ObjectPipelineBuilder, ObjectPresenceFilter, OffloadEngine, PendingTracker, PipelineBuilder, S3PresenceChecker, create_policy_from_config}` → `kvbm_engine::offload::{ObjectPipelineBuilder, ObjectPresenceFilter, OffloadEngine, PendingTracker, PipelineBuilder, S3PresenceChecker, create_policy_from_config}`
- line 504: `crate::v2::distributed::object::ObjectBlockOps` → `kvbm_engine::object::ObjectBlockOps`

**connector/leader/scheduler.rs (lines 7-8, 19):**
- `crate::v2::BlockId` → `kvbm_common::BlockId`
- `crate::v2::logical::blocks::ImmutableBlock` → `kvbm_logical::blocks::ImmutableBlock`
- `crate::v2::integrations::common::{CachedRequestData, NewRequestData, SchedulerOutput}` → `crate::common::{CachedRequestData, NewRequestData, SchedulerOutput}` (these types are LOCAL — see Pitfall 3 in RESEARCH.md)

**connector/worker/mod.rs (lines 59-61):**
- `crate::v2::distributed::worker::{DirectWorker, WorkerTransfers}` → `kvbm_engine::worker::{DirectWorker, WorkerTransfers}`
- `crate::v2::integrations::connector::leader::scheduler::KvConnectorMetadata` → `crate::connector::leader::scheduler::KvConnectorMetadata`
- `crate::v2::integrations::vllm::layout::determine_kv_layout` → `crate::vllm::layout::determine_kv_layout`

**connector/worker/nova/client.rs (lines 11-13):**
- `crate::v2::{BlockId, InstanceId}` → `kvbm_common::BlockId` + `kvbm_engine::InstanceId`
- `crate::v2::distributed::worker::{LeaderLayoutConfig, WorkerLayoutResponse}` → `kvbm_engine::worker::{LeaderLayoutConfig, WorkerLayoutResponse}`

**connector/worker/init/pending.rs (lines 32-40, anything still broken after Pass 2/3):**
- Remaining `crate::v2::distributed::object::create_object_client` → `kvbm_engine::object::create_object_client`
- Remaining `crate::v2::distributed::worker::{DirectWorker, LeaderLayoutConfig, WorkerLayoutResponse}` → `kvbm_engine::worker::{DirectWorker, LeaderLayoutConfig, WorkerLayoutResponse}`

**connector/worker/tests.rs — GATE, do not fix imports:**
Add `#[cfg(all(test, feature = "testing"))]` at the top of the module (or to the `mod tests` declaration in mod.rs). Then comment out the broken `crate::v2::testing::connector::*` import lines with a TODO comment:
```rust
// TODO(Phase 4): Restore when testing infra is ported
// use crate::v2::testing::connector::{ConnectorTestConfig, TestConnectorInstance};
```
Do NOT delete the test file. Do NOT attempt to resolve the testing:: imports in this phase.

After all changes: `cargo check -p kvbm-connector`

If any type is not found at the expected path: run `grep -rn "pub.*TypeName" lib/` to locate it. If genuinely missing across the whole workspace: stop and document as a blocker — do not silently wrap or guess.

Commit: `fix(02-02): Pass 4 — crate::v2::* → workspace crates + gate tests.rs`
  </action>
  <verify>
    <automated>cargo check -p kvbm-connector 2>&1 | grep -E "^error" | grep "v2::" | head -20</automated>
  </verify>
  <done>
No crate::v2:: import paths remain active. connector/worker/tests.rs is gated with cfg attributes and broken imports commented out. cargo check -p kvbm-connector shows no errors from v2 namespace. Pass 4 committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass 5 — Resolve crate::integrations::* self-referential imports (IMP-05)</name>
  <files>
    lib/kvbm-connector/src/connector/leader/init.rs
    lib/kvbm-connector/src/connector/leader/scheduler.rs
    lib/kvbm-connector/src/connector/worker/mod.rs
    lib/kvbm-connector/src/common/output.rs
    lib/kvbm-connector/src/vllm/config.rs
  </files>
  <action>
Fix every remaining `crate::integrations::*` import. This is Pass 5. These are pure self-references — lib.rs IS the old integrations module, so every `crate::integrations::X` becomes `crate::X`.

Note: Some of these may already be fixed in Pass 4 (where integrations:: paths appeared inside v2:: paths). This pass cleans up any remaining standalone `crate::integrations::` uses.

Apply the Pass 5 mapping from RESEARCH.md:

**connector/leader/init.rs (line 11):**
- `crate::integrations::connector::worker::ConnectorWorkerClient` → `crate::connector::worker::ConnectorWorkerClient`

**connector/worker/mod.rs (line 60, if not already fixed in Pass 4):**
- `crate::v2::integrations::connector::leader::scheduler::KvConnectorMetadata` → `crate::connector::leader::scheduler::KvConnectorMetadata`
- `crate::v2::integrations::vllm::layout::determine_kv_layout` → `crate::vllm::layout::determine_kv_layout`

**connector/leader/scheduler.rs (line 7, if not already fixed in Pass 4):**
- `crate::integrations::connector::leader::slot::RequestSlot` → `crate::connector::leader::slot::RequestSlot`

**common/output.rs (line 7, if not already fixed in Pass 4):**
- `crate::v2::integrations::connector::leader::scheduler::KvConnectorMetadata` → `crate::connector::leader::scheduler::KvConnectorMetadata`

**vllm/config.rs (line 12):**
- `crate::v2::integrations::config::{AttentionConfig, IntegrationsConfig, ParallelConfig}` → `crate::config::{AttentionConfig, IntegrationsConfig, ParallelConfig}`

After changes: `cargo check -p kvbm-connector`

At this point the only remaining errors should be from `dynamo_nova`, `dynamo_nova_backend`, and `nova` references — all of which are fixed in Pass 6 (Plan 03). Errors from any other namespace indicate something missed in Passes 1-5.

Commit: `fix(02-02): Pass 5 — crate::integrations::* self-refs → crate::*`
  </action>
  <verify>
    <automated>cargo check -p kvbm-connector 2>&1 | grep -E "^error" | grep -v "dynamo_nova\|nova_backend\|nova::\|::nova\|mod nova" | head -20</automated>
  </verify>
  <done>
No crate::integrations:: import paths remain. cargo check -p kvbm-connector errors are limited exclusively to nova/dynamo_nova namespace (expected — Pass 6 scope). Pass 5 committed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "crate::v2\|crate::integrations" /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger/lib/kvbm-connector/src/` — expect zero matches
2. `grep -r "crate::v2::testing" /home/ryan/repos/dynamo-workspaces/ryan-velo-messenger/lib/kvbm-connector/src/` — expect zero (or only commented-out lines)
3. `cargo check -p kvbm-connector` — only errors should be from nova/dynamo_nova namespace
4. Two commits exist: Pass 4 and Pass 5
</verification>

<success_criteria>
- grep for crate::v2:: and crate::integrations:: returns zero active (uncommented) matches in lib/kvbm-connector/src/
- connector/worker/tests.rs has cfg(all(test, feature = "testing")) gate and broken test imports commented out with Phase 4 TODO
- cargo check -p kvbm-connector errors are exclusively from nova/dynamo_nova namespace
- Two atomic commits created (Pass 4, Pass 5)
</success_criteria>

<output>
After completion, create `.planning/phases/02-import-migration/02-02-SUMMARY.md` following the summary template.
</output>
