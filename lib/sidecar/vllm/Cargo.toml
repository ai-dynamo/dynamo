# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[package]
name = "dynamo-vllm-sidecar"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Rust sidecar for vLLM's native gRPC server"

[package.metadata.cargo-machete]
# Referenced by protobuf code generated into OUT_DIR.
ignored = ["prost"]

[[bin]]
name = "dynamo-vllm-sidecar"
path = "src/main.rs"

# Composition root: the OpenAI frontend with the vLLM direct-gRPC dispatch
# provider registered. Keeps the generic ai-dynamo wheel free of vLLM/tonic.
[[bin]]
name = "dynamo-vllm-frontend"
path = "src/bin/frontend.rs"

[dependencies]
dynamo-backend-common = { workspace = true }
dynamo-llm = { path = "../../llm", default-features = false }
dynamo-runtime = { workspace = true }
dynamo-sidecar-common = { workspace = true }

anyhow = { workspace = true }
async-stream = { workspace = true }
async-trait = { workspace = true }
clap = { version = "4", features = ["derive", "env"] }
futures = { workspace = true }
parking_lot = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
tracing = { workspace = true }

prost = { workspace = true }
prost-types = { workspace = true }
tonic = { workspace = true }

[build-dependencies]
tonic-build = { workspace = true }

[dev-dependencies]
dynamo-backend-common = { workspace = true, features = ["testing"] }
tokio-stream = { workspace = true, features = ["net"] }
